<x-app-layout :title="$title">@include('assessment-module._tabs')<section class="card"><div class="card-body"><h2 class="text-lg font-bold text-emerald-950">Verifikasi Rapor</h2>@forelse($cards as $card)<div class="rounded-2xl border p-4">{{ $card->student?->name }} — {{ $card->classroom?->name }}<div class="mt-2"><button class="btn-primary">Verifikasi</button><button class="btn-secondary">Minta Perbaikan</button><button class="btn-primary">Finalisasi</button></div></div>@empty<div class="empty-state">Tidak ada rapor menunggu verifikasi.</div>@endforelse{{ $cards->links() }}</div></section></x-app-layout>
