<x-app-layout :title="$title">@include('assessment-module._tabs')<section class="card"><div class="card-body"><h2 class="text-lg font-bold text-emerald-950">Leger Nilai</h2><div class="mt-4 flex gap-2"><a class="btn-secondary" href="{{ route('assessments.leger.export') }}">Export CSV</a><a class="btn-primary" href="{{ route('assessments.leger.print') }}">Print</a></div><div class="mt-4 grid gap-3">@forelse($classes as $class)<div class="rounded-2xl border p-4"><b>{{ $class->name }}</b><p>{{ $class->student_enrollments_count }} siswa terdaftar. Nilai belum terverifikasi ditandai pada detail kelas.</p></div>@empty<div class="empty-state">Belum ada kelas.</div>@endforelse</div>{{ $classes->links() }}</div></section></x-app-layout>
