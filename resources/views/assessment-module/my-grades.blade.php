<x-app-layout :title="$title">@include('assessment-module._tabs')<section class="card"><div class="card-body"><h2 class="text-lg font-bold text-emerald-950">Nilai Saya</h2><div class="grid gap-3">@forelse($assignments as $assignment)<a class="rounded-2xl border p-4 hover:bg-emerald-50" href="{{ route('assessments.input', ['teaching_assignment_id'=>$assignment->id]) }}">{{ $assignment->classroom?->name }} — {{ $assignment->subject?->name }}</a>@empty<div class="empty-state">Belum ada penugasan aktif.</div>@endforelse</div>{{ $assignments->links() }}</div></section></x-app-layout>
