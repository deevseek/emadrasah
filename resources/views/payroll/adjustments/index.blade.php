<x-app-layout :title="$title"><div class="space-y-6">@include('payroll.partials.tabs')<div class="card"><div class="card-body">@forelse($adjustments as $a)<p>{{ $a->adjustment_type }} Rp {{ number_format((int)$a->amount,0,',','.') }} — {{ $a->reason }}</p>@empty<div class="empty-state">Belum ada penyesuaian manual.</div>@endforelse{{ $adjustments->links() }}</div></div></div></x-app-layout>
