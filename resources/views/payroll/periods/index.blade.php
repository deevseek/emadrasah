<x-app-layout :title="$title"><div class="space-y-6">@include('payroll.partials.tabs')<div class="flex justify-between"><p>Kelola periode dan cutoff kehadiran.</p>@can('payroll-periods.manage')<a class="btn-primary" href="{{ route('payroll.periods.create') }}">Tambah Periode</a>@endcan</div><div class="card"><div class="card-body"><table class="min-w-full"><tr><th>Kode</th><th>Nama</th><th>Periode</th><th>Status</th><th>Aksi</th></tr>@forelse($periods as $p)<tr><td>{{ $p->period_code }}</td><td>{{ $p->name }}</td><td>{{ $p->period_start_date?->format('d/m/Y') }} - {{ $p->period_end_date?->format('d/m/Y') }}</td><td>{{ $p->status }}</td><td><a href="{{ route('payroll.periods.show',$p) }}">Detail</a> <a href="{{ route('payroll.runs.generate',$p) }}">Generate</a></td></tr>@empty<tr><td colspan="5" class="empty-state">Belum ada periode payroll.</td></tr>@endforelse</table>{{ $periods->links() }}</div></div></div></x-app-layout>
