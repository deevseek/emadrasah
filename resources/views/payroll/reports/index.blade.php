<x-app-layout :title="$title ?? 'Laporan Payroll'"><div class="space-y-6">@include('payroll.partials.tabs')
<x-ui.page-header title="Laporan Payroll" description="Pantau rekap payroll per periode dan status pembayaran pegawai."><x-slot:secondary><x-ui.button variant="outline" :href="route('payroll.reports.export')">Export CSV</x-ui.button><x-ui.button variant="secondary" :href="route('payroll.reports.print')">Print Rekap</x-ui.button></x-slot:secondary></x-ui.page-header>
<div class="grid gap-4 md:grid-cols-2"><x-ui.card><h3 class="mb-4 text-lg font-bold text-emerald-950">Rekap Payroll per Periode</h3>@forelse($runs as $r)<div class="rounded-2xl border border-slate-100 p-4"><p class="font-semibold">{{ $r->period?->name }}</p><p class="text-sm text-slate-600">Total bersih Rp {{ number_format((int)$r->total_net,0,',','.') }}</p></div>@empty<x-ui.empty-state title="Belum ada data laporan" description="Jalankan proses payroll untuk menghasilkan rekap periode." />@endforelse<div class="mt-4">{{ $runs->links() }}</div></x-ui.card><x-ui.card><h3 class="mb-4 text-lg font-bold text-emerald-950">Pembayaran Payroll</h3>@forelse($payments as $p)<div class="rounded-2xl border border-slate-100 p-4"><p class="font-semibold">{{ $p->payment_number }}</p><x-ui.badge>{{ str($p->payment_status)->replace('_',' ')->headline() }}</x-ui.badge></div>@empty<x-ui.empty-state title="Belum ada pembayaran" description="Data pembayaran akan muncul setelah payroll diproses dan dibayarkan." />@endforelse</x-ui.card></div></div></x-app-layout>
