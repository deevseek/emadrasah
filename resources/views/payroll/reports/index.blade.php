<x-app-layout :title="$title"><div class="space-y-6">@include('payroll.partials.tabs')<div class="flex gap-2"><a class="btn-primary" href="{{ route('payroll.reports.export') }}">Export CSV</a><a class="btn-primary" href="{{ route('payroll.reports.print') }}">Print Rekap</a></div><div class="grid gap-4 md:grid-cols-2"><div class="card"><div class="card-body"><h3>Rekap Payroll per Periode</h3>@forelse($runs as $r)<p>{{ $r->period?->name }} — Rp {{ number_format((int)$r->total_net,0,',','.') }}</p>@empty<div class="empty-state">Belum ada data laporan.</div>@endforelse{{ $runs->links() }}</div></div><div class="card"><div class="card-body"><h3>Pembayaran Payroll</h3>@forelse($payments as $p)<p>{{ $p->payment_number }} — {{ $p->payment_status }}</p>@empty<div class="empty-state">Belum ada pembayaran.</div>@endforelse</div></div></div></div></x-app-layout>
