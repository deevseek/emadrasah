<x-app-layout :title="$title"><div class="space-y-6">@include('payroll.partials.tabs')<div class="card"><div class="card-body"><h2>Histori {{ $employee->fullName() }}</h2>@forelse($profiles as $p)<p>{{ $p->effective_start_date?->format('d/m/Y') }} - {{ $p->effective_end_date?->format('d/m/Y') ?? 'sekarang' }}: Rp {{ number_format((int)$p->base_salary,0,',','.') }}</p>@empty<div class="empty-state">Belum ada histori.</div>@endforelse</div></div></div></x-app-layout>
