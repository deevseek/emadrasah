<x-app-layout :title="$title"><div class="space-y-6">@include('payroll.partials.tabs')<div class="card"><div class="card-body"><h2 class="text-xl font-bold">{{ $profile->employee?->fullName() }}</h2><p>Gaji Pokok: Rp {{ number_format((int)$profile->base_salary,0,',','.') }}</p><p>Rekening: {{ $profile->bank_account_number ? '****'.substr($profile->bank_account_number,-4) : '-' }}</p><h3 class="mt-4 font-bold">Komponen</h3>@forelse($profile->components as $c)<p>{{ $c->payrollComponent?->name }} — Rp {{ number_format((int)$c->amount,0,',','.') }}</p>@empty<p class="empty-state">Tidak ada komponen tambahan.</p>@endforelse<a href="{{ route('payroll.salary-profiles.history',$profile->employee) }}">Lihat Histori</a></div></div></div></x-app-layout>
