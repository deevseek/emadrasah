<x-app-layout :title="$title"><div class="space-y-6">@include('payroll.partials.tabs')<div class="card"><div class="card-body"><h2 class="text-xl font-bold">{{ $component->name }}</h2><p>Kode: {{ $component->code }}</p><p>Nominal: Rp {{ number_format((int)$component->default_amount,0,',','.') }}</p>@can('payroll-components.manage')<a href="{{ route('payroll.components.edit',$component) }}">Edit</a><form method="post" action="{{ route('payroll.components.toggle',$component) }}">@csrf @method('patch')<button>Aktif/Nonaktif</button></form>@endcan</div></div></div></x-app-layout>
