<x-app-layout :title="$title"><div class="space-y-6">@include('payroll.partials.tabs')<form method="post" action="{{ route('payroll.runs.store',$period) }}" class="card"><div class="card-body"><h2 class="font-bold">Generate Payroll {{ $period->name }}</h2><p class="text-sm text-slate-600">Pilih pegawai tertentu atau kosongkan untuk seluruh pegawai aktif. Sistem akan melewati pegawai tanpa profil gaji dan menjaga payroll tidak duplikat.</p>@foreach($employees as $e)<label class="block"><input type="checkbox" name="employee_ids[]" value="{{ $e->id }}"> {{ $e->fullName() }}</label>@endforeach @csrf<button class="btn-primary mt-4" onclick="return confirm('Generate payroll sekarang?')">Hitung Payroll</button></div></form></div></x-app-layout>
