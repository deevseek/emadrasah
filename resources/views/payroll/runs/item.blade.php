<x-app-layout :title="$title"><div class="space-y-6">@include('payroll.partials.tabs')<div class="card"><div class="card-body"><h2>{{ $item->employee_name_snapshot }}</h2><p>Periode: {{ $item->run->period?->name }}</p><p>Kehadiran: {{ json_encode($item->attendance_snapshot, JSON_UNESCAPED_UNICODE) }}</p><h3>Rincian Komponen</h3>@foreach($item->components as $c)<p>{{ $c->component_name_snapshot }} ({{ $c->component_type }}) — Rp {{ number_format((int)$c->amount,0,',','.') }}</p>@endforeach<p>Gaji Bersih: Rp {{ number_format((int)$item->net_salary,0,',','.') }}</p>@if($item->run->status==='final')<a href="{{ route('payroll.payslips.show',$item) }}">Cetak Slip</a>@endif<form method="post" action="{{ route('payroll.adjustments.store',$item) }}" class="mt-4">@csrf <select name="adjustment_type"><option value="earning">Penghasilan tambahan</option><option value="deduction">Potongan tambahan</option></select><input name="amount" type="number" placeholder="Nominal"><input name="reason" placeholder="Alasan"><button>Tambah Penyesuaian</button></form></div></div></div></x-app-layout>
