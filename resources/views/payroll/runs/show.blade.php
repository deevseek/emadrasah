<x-app-layout :title="$title"><div class="space-y-6">@include('payroll.partials.tabs')<div class="card"><div class="card-body"><h2>{{ $run->run_number }}</h2><p>Status: {{ $run->status }}</p><p>Total net: Rp {{ number_format((int)$run->total_net,0,',','.') }}</p>@foreach($run->items as $i)<p><a href="{{ route('payroll.runs.items.show',$i) }}">{{ $i->employee_name_snapshot }}</a> — Rp {{ number_format((int)$i->net_salary,0,',','.') }}</p>@endforeach</div></div></div></x-app-layout>
