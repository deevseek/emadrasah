<x-app-layout :title="$title"><div class="space-y-6">@include('payroll.partials.tabs')<div class="card"><div class="card-body">@forelse($items as $i)<p>{{ $i->run?->period?->name }} — {{ $i->payslip_number ?? 'Belum final' }} — Rp {{ number_format((int)$i->net_salary,0,',','.') }} <a href="{{ route('payroll.payslips.mine.show',$i) }}">Lihat Slip</a></p>@empty<div class="empty-state">Belum ada slip gaji untuk akun Anda.</div>@endforelse @if(method_exists($items,'links')){{ $items->links() }}@endif</div></div></div></x-app-layout>
