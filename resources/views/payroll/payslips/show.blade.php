<x-app-layout :title="$title"><div class="space-y-6"><div class="card"><div class="card-body"><h2 class="text-center text-xl font-bold">Slip Gaji</h2><p class="text-center">{{ $profile->school_name }}</p><p>Nomor Slip: {{ $item->payslip_number ?? 'Belum final' }}</p><p>Periode: {{ $item->run->period?->name }}</p><p>Pegawai: {{ $item->employee_name_snapshot }} ({{ $item->employee_number_snapshot }})</p><p>Jabatan: {{ $item->position_snapshot }}</p><h3>Penghasilan</h3>@foreach($item->components->where('component_type','earning') as $c)<p>{{ $c->component_name_snapshot }} — Rp {{ number_format((int)$c->amount,0,',','.') }}</p>@endforeach<h3>Potongan</h3>@foreach($item->components->where('component_type','deduction') as $c)<p>{{ $c->component_name_snapshot }} — Rp {{ number_format((int)$c->amount,0,',','.') }}</p>@endforeach<p>Kehadiran: {{ json_encode($item->attendance_snapshot, JSON_UNESCAPED_UNICODE) }}</p><p>Gaji Kotor: Rp {{ number_format((int)$item->gross_salary,0,',','.') }}</p><p>Total Potongan: Rp {{ number_format((int)$item->total_deductions,0,',','.') }}</p><p class="text-xl font-bold">Gaji Bersih: Rp {{ number_format((int)$item->net_salary,0,',','.') }}</p><a class="print:hidden" href="{{ route('payroll.payslips.print',$item) }}">Print Slip</a></div></div></div></x-app-layout>
