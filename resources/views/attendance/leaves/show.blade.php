<x-app-layout title="Detail Izin"><x-ui.card><p><b>Pegawai:</b> {{ $leave->employee->name }}</p><p><b>Status:</b> {{ $leave->status->value }}</p><p><b>Alasan:</b> {{ $leave->reason }}</p>@if($leave->attachment_path)<a class="text-emerald-800" href="{{ route('employee-leaves.download',$leave) }}">Unduh lampiran</a>@endif<div class="mt-4 flex flex-wrap gap-2"><form method="post" action="{{ route('employee-leaves.approve',$leave) }}">@csrf @method('patch')<button class="rounded bg-emerald-900 px-4 py-2 text-white">Setujui</button></form><form method="post" action="{{ route('employee-leaves.reject',$leave) }}">@csrf @method('patch')<input class="rounded border p-2" name="rejection_reason" placeholder="Alasan penolakan" required><button class="rounded bg-red-700 px-4 py-2 text-white">Tolak</button></form><form method="post" action="{{ route('employee-leaves.cancel',$leave) }}">@csrf @method('patch')<button class="rounded border px-4 py-2">Batalkan</button></form></div></x-ui.card></x-app-layout>
