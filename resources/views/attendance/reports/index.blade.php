<x-app-layout title="Laporan Kehadiran"><div class="space-y-6"><div class="flex items-center justify-between"><div><p class="text-sm text-slate-500">Kehadiran / Laporan</p><h1 class="text-2xl font-semibold text-emerald-950">Laporan Kehadiran</h1></div><a class="rounded-lg bg-emerald-900 px-4 py-2 text-white" href="{{ route('attendance-reports.export', request()->query()) }}">Export CSV</a></div><form class="card card-body grid gap-3 md:grid-cols-5"><input type="date" name="date" value="{{ request('date') }}" class="rounded-lg border-slate-300"><input type="date" name="from" value="{{ request('from') }}" class="rounded-lg border-slate-300"><input type="date" name="to" value="{{ request('to') }}" class="rounded-lg border-slate-300"><select name="employee_id" class="rounded-lg border-slate-300"><option value="">Semua pegawai</option>@foreach($employees as $employee)<option value="{{ $employee->id }}" @selected(request('employee_id')==$employee->id)>{{ $employee->name }}</option>@endforeach</select><select name="status" class="rounded-lg border-slate-300"><option value="">Semua status</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->label() }}</option>@endforeach</select><button class="rounded-lg bg-emerald-900 px-4 py-2 text-white">Filter</button><a class="rounded-lg border px-4 py-2 text-center" href="{{ route('attendance-reports.index') }}">Reset</a></form><section class="grid gap-4 md:grid-cols-4">@foreach($summary as $label=>$value)<div class="card"><div class="card-body"><p class="text-sm text-slate-500">{{ str($label)->replace('_',' ')->headline() }}</p><p class="mt-2 text-xl font-bold text-emerald-950">{{ number_format($value) }}</p></div></div>@endforeach</section><x-ui.card><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr><th>Pegawai</th><th>Tanggal</th><th>Status</th><th>Check-in</th><th>Check-out</th><th>Terlambat</th><th>Pulang awal</th></tr></thead><tbody>@forelse($records as $record)<tr><td>{{ $record->employee?->name }}</td><td>{{ $record->attendance_date?->format('d/m/Y') }}</td><td>{{ $record->status?->label() }}</td><td>{{ $record->checked_in_at?->format('H:i') ?? '-' }}</td><td>{{ $record->checked_out_at?->format('H:i') ?? '-' }}</td><td>{{ $record->late_minutes }}</td><td>{{ $record->early_leave_minutes }}</td></tr>@empty<tr><td colspan="7" class="empty-state">Tidak ada data sesuai filter.</td></tr>@endforelse</tbody></table></div></x-ui.card>{{ $records->links() }}</div></x-app-layout>
