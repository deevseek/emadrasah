<x-app-layout title="Absensi Saya"><div class="grid gap-6 lg:grid-cols-3"><x-ui.card><div class="text-center"><p class="text-sm text-slate-500">Jam realtime</p><p id="clock" class="text-3xl font-bold text-emerald-950"></p><p class="mt-2">Status hari ini: {{ $today?->status?->label() ?? 'Belum absen' }}</p></div><form class="mt-6 space-y-3" method="post" action="{{ route('employee-attendances.check-in') }}" enctype="multipart/form-data">@csrf @foreach(['latitude'=>'Latitude','longitude'=>'Longitude','accuracy'=>'Akurasi','location_text'=>'Alamat/Lokasi'] as $n=>$l)<label class="block">{{ $l }}<input class="w-full rounded border p-2" name="{{ $n }}"></label>@endforeach<label class="block">Selfie<input class="w-full rounded border p-2" type="file" name="selfie" accept="image/*"></label><label class="block">Catatan<textarea class="w-full rounded border p-2" name="notes"></textarea></label><button class="rounded bg-emerald-900 px-4 py-2 text-white">Check-in</button></form><form class="mt-3" method="post" action="{{ route('employee-attendances.check-out') }}">@csrf <button class="rounded border px-4 py-2">Check-out</button></form></x-ui.card><x-ui.card class="lg:col-span-2"><h2 class="font-semibold">Riwayat Pribadi</h2><div class="mt-4 overflow-x-auto"><table class="w-full text-sm"><tr><th>Tanggal</th><th>Masuk</th><th>Pulang</th><th>Status</th></tr>@foreach($records ?? [] as $r)<tr class="border-t"><td>{{ $r->attendance_date->format('d/m/Y') }}</td><td>{{ $r->checked_in_at?->format('H:i') }}</td><td>{{ $r->checked_out_at?->format('H:i') }}</td><td>{{ $r->status->label() }}</td></tr>@endforeach</table></div>{{ $records?->links() }}</x-ui.card></div><script>setInterval(()=>clock.textContent=new Date().toLocaleTimeString('id-ID'),1000)</script></x-app-layout>
