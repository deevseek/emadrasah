<x-app-layout title="Detail Absensi Guru"><x-ui.card><dl class="grid gap-4 md:grid-cols-2"><div><dt>Pegawai</dt><dd>{{ $record->employee->name }}</dd></div><div><dt>Status</dt><dd>{{ $record->status->label() }}</dd></div><div><dt>Lokasi</dt><dd>{{ $record->latitude }}, {{ $record->longitude }} ({{ $record->accuracy }}m) {{ $record->location_text }}</dd></div><div><dt>Selfie</dt><dd>@if($record->selfie_path)<img class="h-40 rounded" src="{{ Storage::url($record->selfie_path) }}">@endif</dd></div></dl><form class="mt-6 space-y-3" method="post" action="{{ route('employee-attendances.verify',$record) }}">@csrf @method('patch')<label>Status<select class="w-full rounded border p-2" name="status">@foreach(\App\Enums\AttendanceStatus::cases() as $s)<option value="{{ $s->value }}">{{ $s->label() }}</option>@endforeach</select></label><label>Alasan Koreksi<textarea class="w-full rounded border p-2" name="correction_reason" required></textarea></label><button class="rounded bg-emerald-900 px-4 py-2 text-white">Verifikasi/Koreksi</button></form></x-ui.card></x-app-layout>
