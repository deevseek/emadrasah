<x-app-layout title="Jurnal Mengajar">
    <div class="mb-6 flex flex-wrap items-center justify-between gap-3"><div><p class="text-sm text-slate-500">Absensi / Jurnal Mengajar</p><h1 class="text-2xl font-semibold text-emerald-950">Jurnal Mengajar</h1></div><a class="rounded-lg bg-emerald-900 px-4 py-2 text-white hover:bg-emerald-800" href="{{ route('teaching-journals.create') }}">Buat Jurnal</a></div>
    @if (session('status'))<div class="mb-4 rounded-lg bg-emerald-50 p-4 text-emerald-900">{{ session('status') }}</div>@endif
    <x-ui.card><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead><tr><th class="px-3 py-2 text-left">Tanggal</th><th class="px-3 py-2 text-left">Guru</th><th class="px-3 py-2 text-left">Kelas</th><th class="px-3 py-2 text-left">Mapel</th><th class="px-3 py-2 text-left">Status</th><th class="px-3 py-2 text-left">Aksi</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse ($journals as $journal)<tr><td class="px-3 py-2">{{ $journal->journal_date->format('d/m/Y') }}</td><td class="px-3 py-2">{{ $journal->employee->name }}</td><td class="px-3 py-2">{{ $journal->classroom->name }}</td><td class="px-3 py-2">{{ $journal->subject->name }}</td><td class="px-3 py-2"><x-ui.badge>{{ $journal->status->value }}</x-ui.badge></td><td class="px-3 py-2"><a class="text-emerald-800 hover:text-emerald-700" href="{{ route('teaching-journals.show', $journal) }}">Detail</a></td></tr>@empty<tr><td class="px-3 py-8 text-center text-slate-500" colspan="6">Belum ada jurnal.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $journals->links() }}</div></x-ui.card>
</x-app-layout>
