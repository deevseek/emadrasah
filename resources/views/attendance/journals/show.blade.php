<x-app-layout title="Detail Jurnal Mengajar"><x-ui.card><div class="print:p-0"><p><b>Guru:</b> {{ $journal->employee->name }}</p><p><b>Kelas/Mapel:</b> {{ $journal->classroom->name }} / {{ $journal->subject->name }}</p><p><b>Waktu:</b> {{ $journal->journal_date->format('d/m/Y') }} {{ $journal->starts_at }}-{{ $journal->ends_at }}</p><p><b>Materi:</b> {{ $journal->material }}</p><p><b>Tujuan:</b> {{ $journal->learning_objectives }}</p><h3 class="mt-4 font-semibold">Kehadiran Siswa</h3><table class="w-full text-sm"><tr><th>Siswa</th><th>Status</th><th>Catatan</th></tr>@foreach($journal->students as $s)<tr class="border-t"><td>{{ $s->name }}</td><td>{{ $s->pivot->status }}</td><td>{{ $s->pivot->notes }}</td></tr>@endforeach</table></div><div class="mt-4 flex flex-wrap gap-2"><button onclick="window.print()" class="rounded border px-4 py-2">Cetak</button><form method="post" action="{{ route('teaching-journals.submit',$journal) }}">@csrf @method('patch')<button class="rounded bg-emerald-900 px-4 py-2 text-white">Kirim</button></form><form method="post" action="{{ route('teaching-journals.verify',$journal) }}">@csrf @method('patch')<button class="rounded bg-emerald-900 px-4 py-2 text-white">Verifikasi</button></form><form method="post" action="{{ route('teaching-journals.reject',$journal) }}">@csrf @method('patch')<input class="rounded border p-2" name="rejection_reason" placeholder="Alasan" required><button class="rounded bg-red-700 px-4 py-2 text-white">Tolak</button></form></div></x-ui.card></x-app-layout>
