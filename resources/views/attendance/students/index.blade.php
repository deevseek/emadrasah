<x-app-layout title="Absensi Siswa"><a class="rounded bg-emerald-900 px-4 py-2 text-white" href="{{ route('student-attendances.create') }}">Input Massal</a><x-ui.card><div class="mt-4 overflow-x-auto"><table class="w-full text-sm"><tr><th>Tanggal</th><th>Kelas</th><th>Siswa</th><th>Status</th></tr>@foreach($records as $r)<tr class="border-t"><td>{{ $r->attendance_date->format('d/m/Y') }}</td><td>{{ $r->classroom->name }}</td><td>{{ $r->student->name }}</td><td>{{ $r->status->label() }}</td></tr>@endforeach</table></div>{{ $records->links() }}</x-ui.card></x-app-layout>
