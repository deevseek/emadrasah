<x-app-layout title="Jadwal Kerja">
<div class="space-y-6"><div class="flex items-center justify-between"><div><p class="text-sm text-slate-500">Kehadiran / Jadwal Kerja</p><h1 class="text-2xl font-semibold text-emerald-950">Jadwal Kerja</h1></div>@can('work-schedules.manage')<a class="rounded-lg bg-emerald-900 px-4 py-2 text-white" href="{{ route('work-schedules.create') }}">Tambah Jadwal</a>@endcan</div><div class="card"><div class="card-body overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead><tr><th class="px-3 py-2 text-left">Nama</th><th class="px-3 py-2 text-left">Hari Kerja</th><th class="px-3 py-2 text-left">Masuk</th><th class="px-3 py-2 text-left">Pulang</th><th class="px-3 py-2 text-left">Jenis Pegawai</th><th class="px-3 py-2 text-left">Status</th><th class="px-3 py-2 text-left">Tindakan</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($schedules as $schedule)<tr><td class="px-3 py-2 font-medium">{{ $schedule->name }}</td><td class="px-3 py-2">{{ collect($schedule->working_days ?? [])->join(', ') }}</td><td class="px-3 py-2">{{ $schedule->check_in_time }} (+{{ $schedule->late_tolerance_minutes }} menit)</td><td class="px-3 py-2">{{ $schedule->check_out_time }}</td><td class="px-3 py-2">{{ $schedule->employee_type ?? 'Semua' }}</td><td class="px-3 py-2"><x-ui.badge>{{ $schedule->is_active ? 'Aktif' : 'Nonaktif' }}</x-ui.badge></td><td class="px-3 py-2"><a class="text-emerald-800" href="{{ route('work-schedules.show',$schedule) }}">Detail</a></td></tr>@empty<tr><td colspan="7" class="empty-state">Belum ada jadwal kerja.</td></tr>@endforelse</tbody></table></div></div>{{ $schedules->links() }}</div>
</x-app-layout>
