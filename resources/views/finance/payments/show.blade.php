<x-layouts.app title="Detail Pembayaran"><x-ui.page-header :title="$payment->payment_number" subtitle="Pembayaran berhasil dan saldo tagihan telah diperbarui."><x-slot:actions><div class="flex flex-wrap gap-2"><x-ui.button :href="route('finance.payments.receipt',$payment)" target="_blank">Lihat Tanda Terima</x-ui.button><x-ui.button :href="route('finance.payments.receipt.pdf',$payment)">Unduh PDF</x-ui.button></div></x-slot:actions></x-ui.page-header>@if(session('success'))<div class="mb-4 rounded border border-emerald-300 bg-emerald-50 p-3 text-emerald-900">{{ session('success') }} Tanda terima telah dibuat.</div>@endif<x-ui.card><dl class="grid gap-3 md:grid-cols-2"><div><dt>Siswa</dt><dd class="font-bold">{{ $payment->student->full_name }}</dd></div><div><dt>Tagihan</dt><dd>{{ $payment->invoice?->invoice_number }}</dd></div><div><dt>Tanggal</dt><dd>{{ $payment->paid_at->translatedFormat('d F Y H:i') }}</dd></div><div><dt>Nominal</dt><dd class="font-bold">Rp {{ number_format((float)$payment->amount,0,',','.') }}</dd></div><div><dt>Metode</dt><dd>{{ str($payment->payment_method)->replace('_',' ')->title() }}</dd></div><div><dt>Referensi</dt><dd>{{ $payment->bank_reference?:'—' }}</dd></div><div><dt>Status Email</dt><dd>@php($states=$payment->receiptDeliveries->pluck('status')->unique()){{ $states->contains('sent')?'Terkirim':($states->contains('failed')?'Gagal':'Belum Dikirim') }}</dd></div></dl><form method="post" action="{{ route('finance.payments.receipt.email',$payment) }}" class="mt-4">@csrf<x-ui.button type="submit">Kirim Ulang Email</x-ui.button></form>@if(blank(config('mail.from.address')))<p class="mt-3 text-amber-700">Email tanda terima belum terkirim karena email sekolah belum dikonfigurasi.</p>@endif</x-ui.card></x-layouts.app>
