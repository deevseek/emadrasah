@component('finance._page', ['title' => 'Pembayaran Siswa'])
<form method="POST" action="{{ route('finance.student-payments.store') }}" class="grid gap-4">@csrf <x-ui.input name="student_id" label="ID Siswa"/><x-ui.input name="payment_date" label="Tanggal" type="date" :value="date('Y-m-d')"/><x-ui.input name="payment_method" label="Metode" value="tunai"/><x-ui.input name="total_amount" label="Total Bayar" type="number"/><div class="rounded bg-gray-50 p-3">@foreach($invoices as $invoice)<label class="block"><input type="checkbox" name="allocations[{{ $loop->index }}][student_invoice_id]" value="{{ $invoice->id }}"> {{ $invoice->student->name }} - {{ $invoice->invoice_number }} <input name="allocations[{{ $loop->index }}][amount]" type="number" placeholder="Alokasi"></label>@endforeach</div><x-ui.button>Posting Pembayaran</x-ui.button></form>
@endcomponent