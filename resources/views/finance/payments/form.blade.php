<x-app-layout title="Catat Pembayaran">
@include('finance.student.tabs')
<form method="POST" action="{{ route('student-finance.payments.store') }}" class="grid gap-4 rounded-2xl bg-white p-6 ring-1 ring-slate-200">@csrf
<x-ui.select name="student_id" label="Siswa" :options="$students->pluck('name','id')"/><div class="grid gap-4 md:grid-cols-3"><x-ui.input name="payment_date" label="Tanggal" type="date" :value="date('Y-m-d')"/><x-ui.select name="payment_method" label="Metode" :options="['tunai'=>'Tunai','transfer_bank'=>'Transfer Bank','lainnya'=>'Lainnya']"/><x-ui.input name="total_amount" label="Total Bayar" type="number"/></div><x-ui.input name="reference_number" label="Nomor Referensi"/><x-ui.input name="notes" label="Catatan"/>
<div class="rounded-2xl bg-slate-50 p-4"><h2 class="mb-3 font-bold text-emerald-950">Alokasi Tagihan</h2>@foreach($invoices as $invoice)<label class="mb-2 grid gap-2 rounded-xl bg-white p-3 md:grid-cols-4"><span>{{ $invoice->student?->name }}</span><span>{{ $invoice->invoice_number }}</span><span>Sisa Rp {{ number_format((int)$invoice->outstanding_amount,0,',','.') }}</span><span><input type="checkbox" name="allocations[{{ $loop->index }}][student_invoice_id]" value="{{ $invoice->id }}"> <input class="rounded border-slate-300" name="allocations[{{ $loop->index }}][amount]" type="number" placeholder="Alokasi"></span></label>@endforeach</div><x-ui.button onclick="return confirm('Simpan pembayaran ini?')">Posting Pembayaran</x-ui.button></form>
</x-app-layout>
