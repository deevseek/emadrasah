<x-app-layout title="{{ isset($invoice) && $invoice->exists ? 'Edit Draft Tagihan' : 'Buat Tagihan Siswa' }}">
@include('finance.student.tabs')
<form method="POST" action="{{ isset($invoice) && $invoice->exists ? route('student-finance.bills.update', $invoice) : route('student-finance.bills.store') }}" class="grid gap-4 rounded-2xl bg-white p-6 ring-1 ring-slate-200 md:grid-cols-2">@csrf @if(isset($invoice) && $invoice->exists) @method('PUT') @endif
<x-ui.select name="student_id" label="Siswa" :options="$students->pluck('name','id')" :value="old('student_id', $invoice->student_id ?? null)"/><x-ui.select name="fee_type_id" label="Jenis Tagihan" :options="$feeTypes->pluck('name','id')" :value="old('fee_type_id', $invoice->fee_type_id ?? null)"/><x-ui.input name="academic_year_id" label="ID Tahun Ajaran" :value="old('academic_year_id', $invoice->academic_year_id ?? null)"/><x-ui.input name="original_amount" label="Nominal" type="number" :value="old('original_amount', $invoice->original_amount ?? null)"/><x-ui.input name="discount_amount" label="Diskon" type="number" value="0"/><x-ui.input name="penalty_amount" label="Penyesuaian" type="number" value="0"/><x-ui.input name="due_on" label="Jatuh Tempo" type="date" :value="old('due_on', isset($invoice) ? $invoice->due_on?->format('Y-m-d') : null)"/><x-ui.textarea name="description" label="Keterangan">{{ old('description', $invoice->description ?? '') }}</x-ui.textarea><div class="md:col-span-2"><x-ui.button>Simpan Tagihan</x-ui.button></div></form>
</x-app-layout>
