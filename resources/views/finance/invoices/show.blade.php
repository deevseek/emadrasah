<x-layouts.app title="Detail Tagihan"><x-ui.page-header :title="$invoice->invoice_number" subtitle="Detail tagihan siswa."/><x-ui.card><dl class="grid gap-3 md:grid-cols-2"><div><dt>Siswa</dt><dd class="font-bold">{{ $invoice->student->full_name }}</dd></div><div><dt>Status</dt><dd><x-ui.badge>{{ ucfirst($invoice->status) }}</x-ui.badge></dd></div><div><dt>Total</dt><dd>Rp {{ number_format((float)$invoice->total,0,',','.') }}</dd></div><div><dt>Sisa</dt><dd>Rp {{ number_format((float)$invoice->outstanding_amount,0,',','.') }}</dd></div></dl><div class="mt-4 flex gap-3">@can('finance.invoice.update')<x-ui.button :href="route('finance.invoices.edit',$invoice)">Ubah</x-ui.button>@endcan @can('finance.invoice.cancel')@if(!in_array($invoice->status,['paid','cancelled']))<form method="post" action="{{ route('finance.invoices.cancel',$invoice) }}">@csrf<x-ui.button type="submit" variant="danger">Batalkan</x-ui.button></form>@endif @endcan</div></x-ui.card></x-layouts.app>
