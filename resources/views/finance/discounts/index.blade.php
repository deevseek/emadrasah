<x-app-layout title="Diskon/Keringanan">
@include('finance.student.tabs')
<form method="POST" action="{{ route('student-finance.discounts.store') }}" class="mb-6 grid gap-4 rounded-2xl bg-white p-5 ring-1 ring-slate-200 md:grid-cols-3">@csrf<x-ui.select name="student_id" label="Siswa" :options="$students->pluck('name','id')"/><x-ui.select name="fee_type_id" label="Jenis" :options="$feeTypes->pluck('name','id')"/><x-ui.input name="academic_year_id" label="ID Tahun Ajaran"/><x-ui.select name="discount_type" label="Tipe" :options="['nominal'=>'Nominal','percentage'=>'Persentase']"/><x-ui.input name="discount_value" label="Nilai" type="number"/><x-ui.input name="reason" label="Alasan"/><div><x-ui.button>Simpan Keringanan</x-ui.button></div></form>
<div class="rounded-2xl bg-white p-5 ring-1 ring-slate-200"><table class="min-w-full text-sm"><tbody>@forelse($discounts as $discount)<tr class="border-t"><td class="py-2">{{ $discount->student?->name }}</td><td>{{ $discount->feeType?->name }}</td><td>Rp {{ number_format((int) $discount->discount_value,0,',','.') }}</td><td>{{ $discount->status }}</td></tr>@empty<tr><td class="py-8 text-center text-slate-500">Belum ada keringanan.</td></tr>@endforelse</tbody></table>{{ $discounts->links() }}</div>
</x-app-layout>
