<x-layouts.app title="Rekonsiliasi BRI"><x-ui.page-header title="Rekonsiliasi BRI" subtitle="Pencocokan transaksi bank dengan tagihan siswa."/>@unless($enabled)<x-ui.card class="mb-4 border-amber-300 bg-amber-50"><x-ui.badge>Integrasi BRI Belum Diaktifkan</x-ui.badge><p class="mt-2 text-sm">Halaman rekonsiliasi tetap tersedia. Aktivasi transaksi langsung menunggu kredensial dan onboarding resmi BRI.</p></x-ui.card>@endunless<x-ui.card><x-ui.table :headers="['Tanggal','External ID','Referensi BRI','VA/Request','Nominal','Status Bank','Status Matching','Aksi']">@forelse($transactions as $t)<tr><td>{{ $t->occurred_at?->translatedFormat('d F Y H:i')??'—' }}</td><td>{{ $t->external_id }}</td><td>{{ $t->provider_reference?:'—' }}</td><td>{{ $t->request_reference?:'—' }}</td><td>Rp {{ number_format((float)$t->amount,0,',','.') }}</td><td>{{ $t->status }}</td><td><x-ui.badge>{{ $t->reconciled_at?'matched':'reconciliation_required' }}</x-ui.badge></td><td>@unless($t->reconciled_at)<form method="post" action="{{ route('finance.bri.reconcile',$t) }}">@csrf<x-ui.button type="submit">Cocokkan</x-ui.button></form>@endunless</td></tr>@empty<tr><td colspan="8"><x-ui.empty-state title="Belum ada transaksi BRI"/></td></tr>@endforelse</x-ui.table>{{ $transactions->links() }}</x-ui.card></x-layouts.app>
