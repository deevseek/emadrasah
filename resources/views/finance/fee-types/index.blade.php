<x-app-layout title="Jenis Tagihan">
<div class="space-y-6">@include('finance.student.tabs')
<x-ui.page-header title="Jenis Tagihan" description="Kelola jenis tagihan siswa, nominal default, frekuensi, dan status penggunaannya."><x-slot:primary>@can('student-fee-types.manage')<x-ui.button :href="route('student-finance.fee-types.create')">Tambah Jenis Tagihan</x-ui.button>@endcan</x-slot:primary></x-ui.page-header>
<x-ui.card><form class="grid gap-3 md:grid-cols-3" method="get"><x-ui.input name="search" label="Pencarian" :value="request('search')"/><div class="flex items-end gap-2"><x-ui.button>Filter</x-ui.button><x-ui.button variant="secondary" :href="route('student-finance.fee-types.index')">Reset</x-ui.button></div></form></x-ui.card>
<x-ui.card><x-ui.table :headers="['Kode','Nama','Kategori','Nominal Default','Frekuensi','Status','Tindakan']">@forelse($feeTypes as $feeType)<tr><td class="font-semibold">{{ $feeType->code }}</td><td>{{ $feeType->name }}</td><td>{{ ['spp'=>'SPP','kegiatan'=>'Kegiatan','daftar_ulang'=>'Daftar Ulang','donasi'=>'Donasi','ujian'=>'Ujian','lainnya'=>'Lainnya','tuition'=>'SPP','activity'=>'Kegiatan','registration'=>'Pendaftaran','other'=>'Lainnya'][$feeType->category] ?? str($feeType->category)->headline() }}</td><td class="text-right">Rp {{ number_format((int)($feeType->default_amount ?? 0),0,',','.') }}</td><td>{{ ['bulanan'=>'Bulanan','sekali'=>'Sekali Bayar','tahunan'=>'Tahunan','semester'=>'Per Semester','insidental'=>'Insidental','monthly'=>'Bulanan','once'=>'Sekali Bayar','annual'=>'Tahunan'][$feeType->billing_frequency ?? ''] ?? str($feeType->billing_frequency ?? '-')->headline() }}</td><td><x-ui.badge :variant="$feeType->is_active ? 'success' : 'muted'">{{ $feeType->is_active ? 'Aktif' : 'Nonaktif' }}</x-ui.badge></td><td><div class="table-actions"><x-ui.button variant="secondary" :href="route('student-finance.fee-types.show', $feeType)">Detail</x-ui.button>@can('student-fee-types.manage')<x-ui.button variant="outline" :href="route('student-finance.fee-types.edit', $feeType)">Edit</x-ui.button><form method="post" action="{{ route('student-finance.fee-types.toggle', $feeType) }}">@csrf @method('patch')<x-ui.button :variant="$feeType->is_active ? 'danger' : 'outline'">{{ $feeType->is_active ? 'Nonaktifkan' : 'Aktifkan' }}</x-ui.button></form>@endcan</div></td></tr>@empty<tr><td colspan="7"><x-ui.empty-state title="Belum ada jenis tagihan" description="Tambahkan jenis tagihan agar tagihan siswa dapat dibuat." />@can('student-fee-types.manage')<div class="mt-3 text-center"><x-ui.button :href="route('student-finance.fee-types.create')">Tambah Jenis Tagihan</x-ui.button></div>@endcan</td></tr>@endforelse</x-ui.table><div class="mt-4">{{ $feeTypes->links() }}</div></x-ui.card>
</div>
</x-app-layout>
