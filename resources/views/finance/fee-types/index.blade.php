@component('finance._page', ['title' => 'Jenis Tagihan'])
<form class="mb-4 grid gap-3 md:grid-cols-3"><x-ui.input name="search" label="Pencarian" :value="request('search')"/><x-ui.button>Filter</x-ui.button></form><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead><tr><th>Kode</th><th>Nama</th><th>Kategori</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($feeTypes as $feeType)<tr><td>{{ $feeType->code }}</td><td>{{ $feeType->name }}</td><td>{{ $feeType->category }}</td><td><x-ui.badge>{{ $feeType->is_active ? 'Aktif' : 'Nonaktif' }}</x-ui.badge></td><td><a href="{{ route('finance.fee-types.show', $feeType) }}">Detail</a></td></tr>@empty<tr><td colspan="5">Belum ada jenis tagihan.</td></tr>@endforelse</tbody></table></div>{{ $feeTypes->links() }}
@endcomponent