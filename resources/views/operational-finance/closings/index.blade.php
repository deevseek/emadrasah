<x-app-layout :title="$title">@include('operational-finance.partials.tabs')<form method="post" action="{{ route('operational-finance.closings.store') }}" class="mb-6 grid gap-3 rounded-2xl bg-white p-5 md:grid-cols-2">@csrf<label>Akun Kas<select name="cash_account_id" class="form-input">@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->name }}</option>@endforeach</select></label><label>Tanggal<input class="form-input" type="date" name="closing_date" value="{{ old('closing_date', '') }}"></label><label>Saldo Aktual<input class="form-input" type="number" name="actual_balance" value="{{ old('actual_balance', '') }}"></label><label>Catatan Selisih<input class="form-input" type="text" name="notes" value="{{ old('notes', '') }}"></label><button class="btn-primary md:col-span-2">Tutup Kas</button></form><div class="rounded-2xl bg-white p-5">@forelse($closings as $c)<div class="border-b py-3">{{ $c->closing_date }} — {{ $c->status }} — Selisih Rp {{ number_format((float)$c->difference,0,',','.') }}</div>@empty<p>Belum ada penutupan kas.</p>@endforelse</div></x-app-layout>
