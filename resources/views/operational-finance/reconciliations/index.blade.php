<x-app-layout :title="$title">@include('operational-finance.partials.tabs')<form method="post" action="{{ route('operational-finance.reconciliations.store') }}" class="mb-6 grid gap-3 rounded-2xl bg-white p-5 md:grid-cols-2">@csrf<label>Akun Kas<select name="cash_account_id" class="form-input">@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->name }}</option>@endforeach</select></label><label>Tanggal Rekonsiliasi<input class="form-input" type="date" name="reconciliation_date" value="{{ old('reconciliation_date', '') }}"></label><label>Saldo Fisik/Rekening<input class="form-input" type="number" name="actual_balance" value="{{ old('actual_balance', '') }}"></label><label>Catatan<input class="form-input" type="text" name="notes" value="{{ old('notes', '') }}"></label><button class="btn-primary md:col-span-2">Simpan Rekonsiliasi</button></form><div class="rounded-2xl bg-white p-5">@forelse($reconciliations as $r)<div class="border-b py-3">{{ $r->reconciliation_date }} — Selisih Rp {{ number_format((float)$r->difference,0,',','.') }}</div>@empty<p>Belum ada rekonsiliasi.</p>@endforelse</div></x-app-layout>
