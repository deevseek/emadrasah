<x-app-layout :title="$title">@include('operational-finance.partials.tabs')<div class="mb-4"><a class="btn-primary" href="{{ route('operational-finance.cash-accounts.create') }}">Tambah Akun Kas</a></div><div class="overflow-x-auto rounded-2xl bg-white shadow-sm"><table class="min-w-full text-sm"><thead><tr class="bg-emerald-900 text-white"><th class="p-3 text-left">Kode</th><th class="p-3 text-left">Nama</th><th class="p-3">Saldo</th><th class="p-3">Status</th></tr></thead><tbody>@forelse($accounts as $a)<tr class="border-b"><td class="p-3">{{ $a->code ?? $a->id }}</td><td class="p-3">{{ $a->name }}</td><td class="p-3 text-right">Rp {{ number_format((float)$a->current_balance,0,',','.') }}</td><td class="p-3 text-center">{{ $a->is_active ? 'Aktif' : 'Nonaktif' }}</td></tr>@empty<tr><td colspan="4" class="p-6 text-center text-slate-500">Belum ada akun kas.</td></tr>@endforelse</tbody></table></div>{{ $accounts->links() }}</x-app-layout>
