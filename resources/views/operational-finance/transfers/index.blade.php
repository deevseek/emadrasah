<x-app-layout :title="$title">@include('operational-finance.partials.tabs')<form method="post" action="{{ route('operational-finance.transfers.store') }}" class="mb-6 grid gap-3 rounded-2xl bg-white p-5 md:grid-cols-3">@csrf<label>Tanggal<input class="form-input" type="date" name="transfer_date" value="{{ old('transfer_date', '') }}"></label><label>Akun Asal<select name="source_cash_account_id" class="form-input">@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->name }}</option>@endforeach</select></label><label>Akun Tujuan<select name="destination_cash_account_id" class="form-input">@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->name }}</option>@endforeach</select></label><label>Nominal<input class="form-input" type="number" name="amount" value="{{ old('amount', '') }}"></label><label>Referensi<input class="form-input" type="text" name="reference_number" value="{{ old('reference_number', '') }}"></label><label>Uraian<input class="form-input" type="text" name="description" value="{{ old('description', '') }}"></label><button class="btn-primary md:col-span-3">Transfer Antar Kas</button></form><div class="rounded-2xl bg-white p-5">@forelse($transfers as $t)<div class="border-b py-3">{{ $t->transfer_number }} — {{ $t->sourceCashAccount?->name }} ke {{ $t->destinationCashAccount?->name }} — Rp {{ number_format((float)$t->amount,0,',','.') }}</div>@empty<p>Belum ada transfer.</p>@endforelse</div>{{ $transfers->links() }}</x-app-layout>
