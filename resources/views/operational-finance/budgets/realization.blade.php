<x-app-layout :title="$title">@include('operational-finance.partials.tabs')<div class="overflow-x-auto rounded-2xl bg-white"><table class="min-w-full text-sm"><thead><tr><th class="p-3">Periode</th><th>Kategori</th><th>Anggaran</th><th>Revisi</th><th>Realisasi</th><th>Sisa</th><th>Status</th></tr></thead><tbody>@forelse($allocations as $a)@php($total=(float)$a->allocated_amount+(float)$a->revised_amount)@php($pct=$total>0?((float)$a->realized_amount/$total*100):0)<tr class="border-t"><td class="p-3">{{ $a->budgetPeriod?->name }}</td><td>{{ $a->financeCategory?->name }}</td><td>Rp {{ number_format((float)$a->allocated_amount,0,',','.') }}</td><td>Rp {{ number_format((float)$a->revised_amount,0,',','.') }}</td><td>Rp {{ number_format((float)$a->realized_amount,0,',','.') }}</td><td>Rp {{ number_format($total-(float)$a->realized_amount,0,',','.') }}</td><td>{{ $pct > 100 ? 'Melebihi Anggaran' : ($pct >= 80 ? 'Mendekati Batas' : 'Aman') }}</td></tr>@empty<tr><td colspan="7" class="p-6 text-center">Belum ada alokasi anggaran.</td></tr>@endforelse</tbody></table></div></x-app-layout>
