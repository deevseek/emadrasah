<x-app-layout :title="$title">@include('operational-finance.partials.tabs')<form method="post" action="{{ route('operational-finance.budgets.store') }}" class="mb-6 grid gap-3 rounded-2xl bg-white p-5 md:grid-cols-3">@csrf<label>Nama Periode<input class="form-input" type="text" name="name" value="{{ old('name', '') }}"></label><label>Tahun Fiskal<input class="form-input" type="number" name="fiscal_year" value="{{ old('fiscal_year', '') }}"></label><label>Total Anggaran<input class="form-input" type="number" name="total_budget" value="{{ old('total_budget', '0') }}"></label><label>Tanggal Mulai<input class="form-input" type="date" name="start_date" value="{{ old('start_date', '') }}"></label><label>Tanggal Selesai<input class="form-input" type="date" name="end_date" value="{{ old('end_date', '') }}"></label><label>Catatan<input class="form-input" type="text" name="notes" value="{{ old('notes', '') }}"></label><button class="btn-primary md:col-span-3">Simpan Anggaran</button></form><a class="btn-secondary" href="{{ route('operational-finance.budgets.realization') }}">Lihat Realisasi Anggaran</a><div class="mt-4 rounded-2xl bg-white p-5">@forelse($periods as $p)<div class="border-b py-3">{{ $p->name }} — Rp {{ number_format((float)$p->total_budget,0,',','.') }} — {{ $p->status }}</div>@empty<p>Belum ada anggaran.</p>@endforelse</div></x-app-layout>
