<x-app-layout :title="$title">@include('operational-finance.partials.tabs')<div class="rounded-2xl bg-white p-5">@forelse($transactions as $t)<div class="flex flex-wrap items-center justify-between gap-3 border-b py-3"><span>{{ $t->transaction_number }} — {{ $t->description }} — Rp {{ number_format((float)$t->amount,0,',','.') }}</span><a class="btn-secondary" href="{{ route('operational-finance.transactions.show',$t) }}">Detail Persetujuan</a></div>@empty<p class="text-slate-500">Tidak ada transaksi menunggu persetujuan.</p>@endforelse</div>{{ $transactions->links() }}</x-app-layout>
