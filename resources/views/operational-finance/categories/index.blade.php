<x-app-layout :title="$title">@include('operational-finance.partials.tabs')<a class="btn-primary" href="{{ route('operational-finance.categories.create') }}">Tambah Kategori</a><div class="mt-4 overflow-x-auto rounded-2xl bg-white"><table class="min-w-full text-sm"><thead><tr><th class="p-3">Kode</th><th>Nama</th><th>Jenis</th><th>Approval</th><th>Status</th></tr></thead><tbody>@forelse($categories as $c)<tr class="border-t"><td class="p-3">{{ $c->code }}</td><td>{{ $c->name }}</td><td>{{ $c->transaction_type === 'income' ? 'Pemasukan' : 'Pengeluaran' }}</td><td>{{ $c->requires_approval ? 'Wajib' : 'Tidak' }}</td><td>{{ $c->is_active ? 'Aktif' : 'Nonaktif' }}</td></tr>@empty<tr><td colspan="5" class="p-6 text-center">Belum ada kategori.</td></tr>@endforelse</tbody></table></div>{{ $categories->links() }}</x-app-layout>
