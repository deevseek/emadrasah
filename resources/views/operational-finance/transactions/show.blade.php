<x-app-layout :title="$title">@include('operational-finance.partials.tabs')<section class="rounded-2xl bg-white p-6 shadow-sm"><h2 class="text-xl font-bold text-emerald-950">{{ $transaction->transaction_number }}</h2><dl class="mt-4 grid gap-3 md:grid-cols-2"><div><dt>Tanggal</dt><dd>{{ $transaction->transaction_date?->format('d/m/Y') }}</dd></div><div><dt>Jenis</dt><dd>{{ $transaction->transaction_type }}</dd></div><div><dt>Akun Kas</dt><dd>{{ $transaction->cashAccount?->name }}</dd></div><div><dt>Kategori</dt><dd>{{ $transaction->category?->name }}</dd></div><div><dt>Nominal</dt><dd>Rp {{ number_format((float)$transaction->amount,0,',','.') }}</dd></div><div><dt>Status</dt><dd>{{ $transaction->status }}</dd></div><div class="md:col-span-2"><dt>Uraian</dt><dd>{{ $transaction->description }}</dd></div></dl><div class="mt-6 flex flex-wrap gap-2">@canany(['operational-incomes.submit','operational-expenses.submit'])<form method="post" action="{{ route('operational-finance.transactions.submit',$transaction) }}">@csrf @method('patch')<button class="btn-secondary">Ajukan</button></form>@endcanany @can('finance-approvals.approve')<form method="post" action="{{ route('operational-finance.transactions.approve',$transaction) }}">@csrf @method('patch')<button class="btn-primary">Setujui</button></form>@endcan @can('finance-approvals.reject')<form method="post" action="{{ route('operational-finance.transactions.reject',$transaction) }}">@csrf @method('patch')<input name="rejection_reason" placeholder="Alasan penolakan" class="form-input"><button class="btn-secondary">Tolak</button></form>@endcan <a class="btn-secondary" href="{{ route('operational-finance.transactions.print',$transaction) }}">Cetak Bukti</a>@canany(['operational-incomes.cancel','operational-expenses.cancel'])<form method="post" action="{{ route('operational-finance.transactions.cancel',$transaction) }}">@csrf @method('patch')<input name="cancellation_reason" placeholder="Alasan pembatalan" class="form-input"><button class="btn-danger">Batalkan</button></form>@endcanany</div></section></x-app-layout>
