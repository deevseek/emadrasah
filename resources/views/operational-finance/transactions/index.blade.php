<x-app-layout :title="$title">@include('operational-finance.partials.tabs')<a class="btn-primary" href="{{ $type==='income' ? route('operational-finance.incomes.create') : route('operational-finance.expenses.create') }}">{{ $type==='income' ? 'Catat Pemasukan' : 'Catat Pengeluaran' }}</a><div class="mt-4 overflow-x-auto rounded-2xl bg-white"><table class="min-w-full text-sm"><thead><tr><th class="p-3">Nomor</th><th>Tanggal</th><th>Akun</th><th>Kategori</th><th>Uraian</th><th>Nominal</th><th>Status</th><th>Tindakan</th></tr></thead><tbody>@forelse($transactions as $t)<tr class="border-t"><td class="p-3">{{ $t->transaction_number }}</td><td>{{ $t->transaction_date?->format('d/m/Y') }}</td><td>{{ $t->cashAccount?->name }}</td><td>{{ $t->category?->name }}</td><td>{{ $t->description }}</td><td>Rp {{ number_format((float)$t->amount,0,',','.') }}</td><td>{{ $t->status }}</td><td><a href="{{ route('operational-finance.transactions.show',$t) }}">Detail</a></td></tr>@empty<tr><td colspan="8" class="p-6 text-center">Belum ada transaksi.</td></tr>@endforelse</tbody></table></div>{{ $transactions->links() }}</x-app-layout>
