<x-app-layout :title="$title">@include('operational-finance.partials.tabs')<form method="post" action="{{ $type==='income' ? route('operational-finance.incomes.store') : route('operational-finance.expenses.store') }}" class="grid gap-4 rounded-2xl bg-white p-6 md:grid-cols-2">@csrf<label>Tanggal<input class="form-input" type="date" name="transaction_date" value="{{ old('transaction_date', '') }}"></label><label>Akun Kas<select name="cash_account_id" class="form-input">@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->name }}</option>@endforeach</select></label><label>Kategori<select name="finance_category_id" class="form-input">@foreach($categories as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</select></label><label>Nominal<input class="form-input" type="number" name="amount" value="{{ old('amount', '') }}"></label><label>Nomor Referensi<input class="form-input" type="text" name="reference_number" value="{{ old('reference_number', '') }}"></label><label class="md:col-span-2">Uraian<textarea name="description" class="form-input" required>{{ old('description') }}</textarea></label><label class="md:col-span-2">Catatan<textarea name="notes" class="form-input">{{ old('notes') }}</textarea></label><label><input type="checkbox" name="post_now" value="1"> Langsung bukukan bila berizin</label><button class="btn-primary md:col-span-2">Simpan Draft</button></form></x-app-layout>
