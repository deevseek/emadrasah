<x-app-layout title="Data Orang Tua/Wali"><div class="space-y-6"><div class="flex justify-between gap-3"><div><h2 class="text-2xl font-black text-emerald-950">Data Orang Tua/Wali</h2><p class="text-sm text-slate-500">Kontak keluarga dan penanggung jawab siswa.</p></div>@can('guardians.create')<a class="btn btn-primary" href="{{ route('guardians.create') }}">Tambah Wali</a>@endcan</div><form class="card card-body grid gap-3 md:grid-cols-4"><input name="search" value="{{ request('search') }}" placeholder="Cari nama, NIK, telepon" class="md:col-span-2"><select name="status"><option value="">Semua status</option><option value="active" @selected(request('status')==='active')>Aktif</option><option value="inactive" @selected(request('status')==='inactive')>Nonaktif</option></select><button class="btn btn-primary">Filter</button></form>@if($guardians->isEmpty())<div class="empty-state">Belum ada data wali.</div>@else<div class="table-wrap"><table class="data-table"><thead><tr><th>Nama</th><th>NIK</th><th>Telepon</th><th>Pekerjaan</th><th>Jumlah Anak</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@foreach($guardians as $guardian)<tr><td class="font-semibold">{{ $guardian->name }}</td><td>{{ $guardian->national_identity_number }}</td><td>{{ $guardian->phone ?? '-' }}</td><td>{{ $guardian->occupation ?? '-' }}</td><td>{{ $guardian->students_count ?? $guardian->students()->count() }}</td><td><span @class(['badge',$guardian->is_active?'badge-success':'badge-muted'])>{{ $guardian->is_active?'Aktif':'Nonaktif' }}</span></td><td><div class="flex gap-2"><a class="btn btn-secondary px-3 py-1.5" href="{{ route('guardians.show',$guardian) }}">Detail</a>@can('guardians.update')<a class="btn btn-secondary px-3 py-1.5" href="{{ route('guardians.edit',$guardian) }}">Edit</a>@endcan</div></td></tr>@endforeach</tbody></table></div>{{ $guardians->links() }}@endif</div></x-app-layout>
