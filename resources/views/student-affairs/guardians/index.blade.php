<x-app-layout><h1 class="text-2xl font-bold text-emerald-950">Data Wali</h1><form><input name="search" value="{{ request('search') }}" placeholder="Cari wali"><button>Filter</button><a href="{{ route('guardians.create') }}">Tambah</a></form><table class="w-full bg-white"><tbody>@forelse($guardians as $guardian)<tr><td>{{ $guardian->name }}</td><td>{{ $guardian->phone }}</td><td>{{ $guardian->is_active ? 'Aktif' : 'Tidak Aktif' }}</td><td><a href="{{ route('guardians.show',$guardian) }}">Detail</a> <a href="{{ route('guardians.edit',$guardian) }}">Edit</a></td></tr>@empty<tr><td>Belum ada data wali.</td></tr>@endforelse</tbody></table>{{ $guardians->links() }}</x-app-layout>
