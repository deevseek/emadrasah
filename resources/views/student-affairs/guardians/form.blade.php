<x-app-layout><h1>{{ $guardian->exists ? 'Edit Wali' : 'Tambah Wali' }}</h1><form method="POST" action="{{ $guardian->exists ? route('guardians.update',$guardian) : route('guardians.store') }}" class="grid gap-3 bg-white p-6">@csrf @if($guardian->exists) @method('PUT') @endif<input name="name" value="{{ old('name',$guardian->name) }}" placeholder="Nama wali"><input name="national_identity_number" value="{{ old('national_identity_number',$guardian->national_identity_number) }}" placeholder="NIK"><select name="gender"><option value="">-</option>@foreach($genders as $gender)<option value="{{ $gender->value }}" @selected(old('gender',$guardian->gender?->value)===$gender->value)>{{ $gender->label() }}</option>@endforeach</select><input name="phone" value="{{ old('phone',$guardian->phone) }}" placeholder="Telepon"><input name="monthly_income" value="{{ old('monthly_income',$guardian->monthly_income) }}" placeholder="Penghasilan"><textarea name="address">{{ old('address',$guardian->address) }}</textarea><select name="user_id"><option value="">Tanpa akun</option>@foreach($users as $user)<option value="{{ $user->id }}" @selected(old('user_id',$guardian->user_id)==$user->id)>{{ $user->name }}</option>@endforeach</select>@foreach($errors->all() as $error)<p class="text-red-600">{{ $error }}</p>@endforeach<button>Simpan</button></form></x-app-layout>
