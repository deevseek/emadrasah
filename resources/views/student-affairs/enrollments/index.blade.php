<x-app-layout title="Penempatan Kelas"><div class="space-y-6"><div class="flex justify-between gap-3"><div><h2 class="text-2xl font-black text-emerald-950">Penempatan Kelas</h2><p class="text-sm text-slate-500">Pantau dan kelola siswa pada rombel aktif.</p></div>@can('student-enrollments.create')<a class="btn btn-primary" href="{{ route('student-enrollments.create') }}">Tempatkan Siswa</a>@endcan</div><form class="card card-body grid gap-3 md:grid-cols-4"><input name="search" value="{{ request('search') }}" placeholder="Cari siswa atau NIS" class="md:col-span-2"><button class="btn btn-primary">Filter</button></form>@if($enrollments->isEmpty())<div class="empty-state">Belum ada penempatan kelas.</div>@else<div class="table-wrap"><table class="data-table"><thead><tr><th>Siswa</th><th>NIS</th><th>Tahun Ajaran</th><th>Kelas</th><th>Tanggal Masuk</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@foreach($enrollments as $enrollment)<tr><td class="font-semibold">{{ $enrollment->student?->name }}</td><td>{{ $enrollment->student?->student_number }}</td><td>{{ $enrollment->academicYear?->name }}</td><td>{{ $enrollment->classroom?->name }}</td><td>{{ $enrollment->enrolled_at?->format('d/m/Y') }}</td><td><span class="badge badge-success">{{ str($enrollment->enrollment_status)->headline() }}</span></td><td class="flex gap-2">@can('student-enrollments.transfer')<button class="btn btn-secondary px-3 py-1.5">Transfer</button>@endcan @can('student-enrollments.delete')<form method="post" action="{{ route('student-enrollments.destroy',$enrollment) }}" onsubmit="return confirm('Keluarkan siswa dari kelas ini?')">@csrf @method('delete')<button class="btn btn-danger px-3 py-1.5">Keluarkan</button></form>@endcan</td></tr>@endforeach</tbody></table></div>{{ $enrollments->links() }}@endif</div></x-app-layout>
