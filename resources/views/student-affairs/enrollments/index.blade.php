<x-app-layout><h1 class="text-2xl font-bold text-emerald-950">Penempatan Kelas</h1><form method="GET"><select name="academic_year_id"><option value="">Semua tahun ajaran</option>@foreach($academicYears as $year)<option value="{{ $year->id }}" @selected(request('academic_year_id')==$year->id)>{{ $year->name }}</option>@endforeach</select><select name="classroom_id"><option value="">Semua kelas</option>@foreach($classrooms as $classroom)<option value="{{ $classroom->id }}" @selected(request('classroom_id')==$classroom->id)>{{ $classroom->name }} ({{ $classroom->studentEnrollments()->where('enrollment_status','active')->count() }}/{{ $classroom->capacity ?? '∞' }})</option>@endforeach</select><button>Filter</button></form><section class="bg-white p-4"><h2>Tambah siswa ke kelas</h2><form method="POST" action="{{ route('student-enrollments.store') }}">@csrf<select name="student_id">@foreach($students as $student)<option value="{{ $student->id }}">{{ $student->name }}</option>@endforeach</select><select name="academic_year_id">@foreach($academicYears as $year)<option value="{{ $year->id }}">{{ $year->name }}</option>@endforeach</select><select name="classroom_id">@foreach($classrooms as $classroom)<option value="{{ $classroom->id }}">{{ $classroom->name }}</option>@endforeach</select><button>Tempatkan</button></form></section><table class="w-full bg-white"><thead><tr><th>Siswa</th><th>Tahun Ajaran</th><th>Kelas</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@foreach($enrollments as $enrollment)<tr><td>{{ $enrollment->student->name }}</td><td>{{ $enrollment->academicYear->name }}</td><td>{{ $enrollment->classroom->name }}</td><td>{{ $enrollment->enrollment_status->label() }}</td><td><form method="POST" action="{{ route('student-enrollments.transfer',$enrollment) }}">@csrf<select name="classroom_id">@foreach($classrooms as $classroom)<option value="{{ $classroom->id }}">{{ $classroom->name }}</option>@endforeach</select><button>Pindah</button></form><form method="POST" action="{{ route('student-enrollments.destroy',$enrollment) }}">@csrf @method('DELETE')<button>Keluarkan</button></form></td></tr>@endforeach</tbody></table>{{ $enrollments->links() }}</x-app-layout>
