<x-app-layout title="Data Siswa">
<div class="space-y-6"><div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 class="text-2xl font-black text-emerald-950">Data Siswa</h2><p class="text-sm text-slate-500">Kelola identitas, wali, status, dan kelas aktif siswa.</p></div>@can('students.create')<a href="{{ route('students.create') }}" class="btn btn-primary">Tambah Siswa</a>@endcan</div>
<form class="card card-body grid gap-3 md:grid-cols-5"><input name="search" value="{{ request('search') }}" placeholder="Cari nama, NIS, NISN, NIK" class="md:col-span-2"><select name="status"><option value="">Semua status</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->label() }}</option>@endforeach</select><select name="gender"><option value="">Semua gender</option>@foreach($genders as $gender)<option value="{{ $gender->value }}" @selected(request('gender')===$gender->value)>{{ $gender->label() }}</option>@endforeach</select><button class="btn btn-primary">Filter</button></form>
@if($students->isEmpty())<div class="empty-state">Belum ada data siswa sesuai filter.</div>@else<div class="table-wrap"><table class="data-table"><thead><tr><th>Foto</th><th>NIS</th><th>NISN</th><th>Nama</th><th>Jenis Kelamin</th><th>Kelas Aktif</th><th>Status</th><th>Wali Utama</th><th>Aksi</th></tr></thead><tbody>@foreach($students as $student)<tr><td>@if($student->photo_path)<img src="{{ Storage::url($student->photo_path) }}" class="h-10 w-10 rounded-full object-cover">@else<div class="grid h-10 w-10 place-items-center rounded-full bg-emerald-100 font-bold text-emerald-800">{{ str($student->name)->substr(0,2)->upper() }}</div>@endif</td><td>{{ $student->student_number }}</td><td>{{ $student->national_student_number }}</td><td class="font-semibold">{{ $student->name }}</td><td>{{ $student->gender?->label() }}</td><td>{{ $student->activeEnrollment?->classroom?->name ?? '-' }}</td><td><span class="badge badge-success">{{ $student->student_status->label() }}</span></td><td>{{ $student->guardians->first()?->name ?? '-' }}</td><td><div class="flex gap-2"><a class="btn btn-secondary px-3 py-1.5" href="{{ route('students.show',$student) }}">Detail</a>@can('students.update')<a class="btn btn-secondary px-3 py-1.5" href="{{ route('students.edit',$student) }}">Edit</a>@endcan</div></td></tr>@endforeach</tbody></table></div>{{ $students->links() }}@endif</div>
</x-app-layout>
