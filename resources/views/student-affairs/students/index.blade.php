<x-app-layout><div class="space-y-6"><h1 class="text-2xl font-bold text-emerald-950">Data Siswa</h1><form><input name="search" value="{{ request('search') }}" placeholder="Cari nama, NIS, NISN, NIK" class="rounded border-gray-300"><select name="status"><option value="">Semua status</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ $status->label() }}</option>@endforeach</select><select name="gender"><option value="">Semua gender</option>@foreach($genders as $gender)<option value="{{ $gender->value }}" @selected(request('gender')===$gender->value)>{{ $gender->label() }}</option>@endforeach</select><button class="rounded bg-emerald-900 px-4 py-2 text-white">Filter</button>@can('students.create')<a href="{{ route('students.create') }}" class="rounded bg-amber-500 px-4 py-2 text-white">Tambah</a>@endcan</form><div class="overflow-x-auto bg-white"><table class="w-full"><thead><tr><th>Foto</th><th>Nama</th><th>NIS/NISN</th><th>Status</th><th>Kelas Aktif</th><th>Aksi</th></tr></thead><tbody>@forelse($students as $student)<tr><td>@if($student->photo_path)<img src="{{ Storage::url($student->photo_path) }}" class="h-10 w-10 rounded-full">@else<div class="h-10 w-10 rounded-full bg-gray-200"></div>@endif</td><td>{{ $student->name }}</td><td>{{ $student->student_number }} / {{ $student->national_student_number }}</td><td><span class="rounded bg-emerald-100 px-2 py-1">{{ $student->student_status->label() }}</span></td><td>{{ $student->activeEnrollment->first()?->classroom?->name ?? '-' }}</td><td><a href="{{ route('students.show',$student) }}">Detail</a> @can('students.update')<a href="{{ route('students.edit',$student) }}">Edit</a>@endcan</td></tr>@empty<tr><td colspan="6">Belum ada data siswa.</td></tr>@endforelse</tbody></table></div>{{ $students->links() }}</div></x-app-layout>
