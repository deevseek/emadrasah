<x-layouts.app title="Kelola Konten Website"><x-ui.page-header title="{{ ucfirst($type) }}" description="Tambah, ubah, urutkan, aktifkan, atau hapus konten."><x-slot:actions><x-ui.button :href="route('website.content.create',$type)">Tambah Konten</x-ui.button></x-slot:actions></x-ui.page-header><x-ui.card><div class="overflow-x-auto"><table class="w-full text-left"><thead><tr class="border-b"><th class="p-3">Judul/Nama</th><th>Status</th><th class="text-right">Aksi</th></tr></thead><tbody>@forelse($items as $item)<tr class="border-b"><td class="p-3 font-semibold">{{ $item->title??$item->name }}</td><td>{{ ($item->status?->value??$item->status??($item->is_active?'Aktif':'Nonaktif')) }}</td><td class="flex justify-end gap-2 p-3"><x-ui.button size="sm" variant="secondary" :href="route('website.content.edit',[$type,$item->id])">Edit</x-ui.button><form method="post" action="{{ route('website.content.destroy',[$type,$item->id]) }}" onsubmit="return confirm('Hapus konten ini?')">@csrf @method('DELETE')<x-ui.button size="sm" variant="danger" type="submit">Hapus</x-ui.button></form></td></tr>@empty<tr><td colspan="3" class="p-8 text-center text-slate-500">Belum ada konten.</td></tr>@endforelse</tbody></table></div>{{ $items->links() }}</x-ui.card></x-layouts.app>
