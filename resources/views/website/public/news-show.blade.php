<x-layouts.public :title="$item->title" :description="$item->excerpt"><article class="section"><div class="wrap max-w-4xl"><a href="{{ route('public.news') }}" class="text-emerald-700">← Semua berita</a><p class="mt-8 gold">{{ $item->category }} · {{ $item->published_at->translatedFormat('d F Y') }}</p><h1 class="serif mt-3 text-5xl font-bold">{{ $item->title }}</h1>@if($item->featured_image)<img src="{{ asset('storage/'.$item->featured_image) }}" class="mt-8 max-h-[500px] w-full rounded-3xl object-cover" alt="{{ $item->title }}">@endif<div class="mt-8 whitespace-pre-line text-lg leading-8 text-slate-700">{{ $item->content }}</div></div></article></x-layouts.public>
