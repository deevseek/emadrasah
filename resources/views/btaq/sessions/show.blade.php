<x-module-page title="Detail Jurnal BTAQ"><div class="rounded-xl bg-white p-6 shadow"><h2 class="text-xl font-bold">{{ $session->topic ?: 'Jurnal BTAQ' }}</h2><p>{{ $session->session_date?->format('d F Y') }} — {{ $session->status }}</p><p class="mt-3">{{ $session->learning_activity }}</p><div class="mt-4 flex gap-2"><a class="rounded border px-4 py-2" href="{{ route('btaq-sessions.edit',$session) }}">Edit</a><form method="POST" action="{{ route('btaq-sessions.submit',$session) }}">@csrf @method('PATCH')<button class="rounded bg-emerald-900 px-4 py-2 text-white">Simpan dan Kirim</button></form><a class="rounded border px-4 py-2" href="{{ route('btaq-sessions.print',$session) }}">Cetak</a></div></div></x-module-page>
