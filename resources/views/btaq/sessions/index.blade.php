<x-module-page title="Jurnal BTAQ"><div class="rounded-xl bg-white p-6 shadow"><a href="{{ route('btaq-sessions.create') }}" class="rounded bg-emerald-900 px-4 py-2 text-white">Tambah Jurnal</a>@foreach($sessions as $session)<p class="border-t py-3"><a class="text-emerald-800" href="{{ route('btaq-sessions.show',$session) }}">{{ $session->session_date?->format('d/m/Y') }} — {{ $session->group?->name }} — {{ $session->status }}</a></p>@endforeach{{ $sessions->links() }}</div></x-module-page>
