<x-module-page title="BTAQ Saya" subtitle="Jadwal, kelompok, pekerjaan tertunda, dan riwayat sesi BTAQ."><div class="grid gap-4 lg:grid-cols-3"><section class="rounded-xl bg-white p-5 shadow"><h2 class="font-bold text-emerald-950">Jadwal Hari Ini</h2>@forelse($schedules as $schedule)<p class="mt-2">{{ $schedule->start_time }} — {{ $schedule->group?->name }} <a class="text-emerald-800" href="{{ route('btaq-sessions.create',['schedule_id'=>$schedule->id]) }}">Isi Jurnal</a></p>@empty<p class="mt-3 text-slate-500">Tidak ada jadwal hari ini.</p>@endforelse</section><section class="rounded-xl bg-white p-5 shadow"><h2 class="font-bold text-emerald-950">Kelompok Saya</h2>@foreach($groups as $group)<p class="mt-2">{{ $group->name }} ({{ $group->students_count }} siswa)</p>@endforeach</section><section class="rounded-xl bg-white p-5 shadow"><h2 class="font-bold text-emerald-950">Pekerjaan Tertunda</h2><p class="text-slate-600">Gunakan tombol Isi Jurnal untuk menyimpan draft, absensi, dan perkembangan siswa.</p></section></div><div class="mt-4 rounded-xl bg-white p-5 shadow"><h2 class="font-bold">Riwayat Sesi</h2>@foreach($sessions as $session)<p class="border-t py-2"><a href="{{ route('btaq-sessions.show',$session) }}" class="text-emerald-800">{{ $session->session_date?->format('d/m/Y') }} — {{ $session->topic ?: 'Jurnal BTAQ' }}</a></p>@endforeach{{ $sessions->links() }}</div></x-module-page>
