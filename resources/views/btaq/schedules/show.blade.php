<x-module-page title="Detail Jadwal BTAQ"><div class="rounded-xl bg-white p-6 shadow"><p class="font-semibold">{{ $schedule->group?->name }} — {{ $schedule->start_time }} sampai {{ $schedule->end_time }}</p><p>Guru: {{ $schedule->teacher?->name }}</p><div class="mt-4 flex gap-2"><a class="rounded border px-4 py-2" href="{{ route('btaq-schedules.edit',$schedule) }}">Edit</a><a class="rounded bg-emerald-900 px-4 py-2 text-white" href="{{ route('btaq-sessions.create',['schedule_id'=>$schedule->id]) }}">Isi Jurnal</a></div></div></x-module-page>
