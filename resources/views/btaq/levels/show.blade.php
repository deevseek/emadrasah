<x-module-page title="Detail Level BTAQ"><div class="rounded-xl bg-white p-6 shadow"><p class="text-lg font-semibold">{{ $level->name }}</p><p>{{ $level->description ?: 'Tidak ada deskripsi.' }}</p><a href="{{ route('btaq-levels.edit',$level) }}" class="mt-4 inline-block rounded bg-emerald-800 px-4 py-2 text-white">Edit</a><form method="POST" action="{{ route('btaq-levels.toggle',$level) }}" class="inline">@csrf @method('PATCH')<button class="rounded border px-4 py-2">Aktif/Nonaktif</button></form></div></x-module-page>
