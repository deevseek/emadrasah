<x-module-page title="Verifikasi Jurnal BTAQ"><div class="rounded-xl bg-white p-6 shadow">@forelse($sessions as $session)<div class="border-t py-4"><p class="font-semibold">{{ $session->group?->name }} — {{ $session->session_date?->format('d/m/Y') }}</p><form method="POST" action="{{ route('btaq-verifications.verify',$session) }}" class="inline">@csrf @method('PATCH')<button class="rounded bg-emerald-900 px-3 py-2 text-white">Verifikasi</button></form><form method="POST" action="{{ route('btaq-verifications.reject',$session) }}" class="mt-2 inline-flex gap-2">@csrf @method('PATCH')<input name="rejection_reason" class="rounded border-slate-300" placeholder="Alasan perbaikan" required><button class="rounded border px-3 py-2">Minta Perbaikan</button></form></div>@empty<p class="text-slate-500">Tidak ada jurnal menunggu verifikasi.</p>@endforelse{{ $sessions->links() }}</div></x-module-page>
