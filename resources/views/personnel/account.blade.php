<x-layouts.app :title="$title" :breadcrumbs="['Beranda','Data Personalia','Hubungkan Akun']"><x-ui.card><h1 class="text-xl font-black text-emerald-950">Hubungkan Akun</h1><p class="mt-2">Personalia: <strong>{{ $personnel->full_name }}</strong></p><p>Akun saat ini: {{ $personnel->user?->name ?: 'Belum terhubung' }}</p><form method="post" action="{{ route('personnel.account.update',$personnel) }}" class="mt-5">@csrf @method('PATCH')<select name="user_id" class="input"><option value="">Pilih akun baru</option>@foreach($accounts as $a)<option value="{{ $a->id }}">{{ $a->name }} — {{ $a->email }}</option>@endforeach</select><div class="mt-4 flex gap-2"><a class="btn btn-secondary" href="{{ route('personnel.show',$personnel) }}">Batal</a><button class="btn btn-primary">Hubungkan Akun</button></div></form>@if($personnel->user)<form method="post" action="{{ route('personnel.account.destroy',$personnel) }}" class="mt-4">@csrf @method('DELETE')<button class="btn btn-danger">Lepaskan Akun</button></form>@endif</x-ui.card></x-layouts.app>
