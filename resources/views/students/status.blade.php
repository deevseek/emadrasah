<x-layouts.app :title="$title" :breadcrumbs="['Beranda','Kesiswaan','Data Siswa','Ubah Status']"><form method="post" action="{{ route('students.status.update',$student) }}" class="mx-auto max-w-2xl space-y-5">@csrf @method('PATCH')<x-ui.page-header :title="$title" :description="$student->full_name"/><x-ui.card><label class="label">Status Baru</label><select class="input" name="status">@foreach(config('students.statuses') as $k=>$v)<option value="{{ $k }}" @selected(old('status',$student->status)===$k)>{{ $v }}</option>@endforeach</select><label class="label mt-4">Keterangan Perubahan</label><textarea class="input" name="note" maxlength="1000">{{ old('note') }}</textarea><p class="mt-4 text-sm text-slate-600">Data siswa tidak akan dihapus. Siswa tidak lagi dihitung sebagai siswa aktif bila status bukan Aktif.</p></x-ui.card><div class="flex justify-end gap-2"><a class="btn btn-secondary" href="{{ route('students.show',$student) }}">Batal</a><button class="btn btn-primary">Ubah Status</button></div></form></x-layouts.app>
