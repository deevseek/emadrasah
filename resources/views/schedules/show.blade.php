<x-app-layout><x-slot name="header"><h1 class="text-xl font-semibold text-emerald-950">Detail Jadwal Pelajaran</h1></x-slot><div class="rounded-xl bg-white p-6 shadow"><p>{{ $schedule->day_of_week?->label() }}, {{ substr($schedule->starts_at,0,5) }}–{{ substr($schedule->ends_at,0,5) }}</p><p>{{ $schedule->classroom?->name }} · {{ $schedule->subject?->name }} · {{ $schedule->employee?->name ?? 'Belum ditetapkan' }} · {{ $schedule->room ?: '-' }}</p><dl class="mt-4 grid gap-2 text-sm"><div><dt class="font-semibold">Sesi bersama</dt><dd>{{ $schedule->isSharedSession() ? 'Ya' : 'Tidak' }}</dd></div>@if($schedule->isSharedSession())<div><dt class="font-semibold">Nama sesi</dt><dd>{{ $schedule->shared_session_name }}</dd></div><div><dt class="font-semibold">Kelas peserta</dt><dd>{{ $participants->join(', ') }}</dd></div>@endif</dl></div></x-app-layout>
