<x-layouts.app :title="$title" :breadcrumbs="['HRD','Izin & Cuti']"><div class="grid gap-4 lg:grid-cols-3"><x-ui.card><h2 class="font-bold">Ajukan Izin/Cuti</h2><form method="post" action="{{ route('hrd.leave.store') }}" enctype="multipart/form-data" class="mt-4 space-y-3">@csrf<div><label>Mulai</label><input type="date" name="start_date" required></div><div><label>Selesai</label><input type="date" name="end_date" required></div><div><label>Jenis</label><select name="leave_type">@foreach(\App\Enums\Hrd\LeaveType::cases() as $t)<option value="{{ $t->value }}">{{ $t->label() }}</option>@endforeach</select></div><div><label>Alasan</label><textarea name="reason" required></textarea></div><div><label>Lampiran (JPG/PNG/PDF)</label><input type="file" name="attachment" accept=".jpg,.jpeg,.png,.pdf"></div><button class="btn btn-primary">Kirim Pengajuan</button></form></x-ui.card><x-ui.card class="overflow-x-auto !p-0 lg:col-span-2"><table class="w-full text-sm"><thead><tr><th>Personalia</th><th>Periode</th><th>Jenis</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@foreach($leaves as $l)<tr class="border-t"><td class="p-3">{{ $l->personnel->full_name }}</td><td>{{ $l->start_date->format('d/m/Y') }}–{{ $l->end_date->format('d/m/Y') }}</td><td>{{ $l->leave_type->label() }}</td><td><span class="badge">{{ $l->status }}</span></td><td>@if($l->status==='pending')@can('personnel-leave.approve')<form class="inline" method="post" action="{{ route('hrd.leave.approve',$l) }}">@csrf<button class="text-emerald-700">Setujui</button></form><form class="inline" method="post" action="{{ route('hrd.leave.reject',$l) }}">@csrf<input type="hidden" name="rejection_note" value="Ditolak oleh pejabat berwenang"><button class="text-rose-700">Tolak</button></form>@else<form method="post" action="{{ route('hrd.leave.cancel',$l) }}">@csrf<button>Batalkan</button></form>@endcan @endif</td></tr>@endforeach</tbody></table><div class="p-4">{{ $leaves->links() }}</div></x-ui.card></div></x-layouts.app>
