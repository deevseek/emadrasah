<x-app-layout title="Detail Tahun Ajaran"><div class="space-y-6"><section class="card"><div class="card-body"><h2 class="text-xl font-bold text-emerald-950">{{ $year->name }}</h2><p class="text-sm text-slate-500">{{ $year->starts_on->format('d/m/Y') }} - {{ $year->ends_on->format('d/m/Y') }}</p><span @class(['badge','mt-3',$year->is_active?'badge-success':'badge-muted'])>{{ $year->is_active?'Aktif':'Tidak Aktif' }}</span><div class="mt-4 flex gap-2"><a class="btn btn-secondary" href="{{ route('academic-years.index') }}">Kembali</a>@can('academic-years.update')<a class="btn btn-primary" href="{{ route('academic-years.edit',$year) }}">Ubah</a>@endcan</div></div></section><section class="card"><div class="card-body"><h3 class="font-bold text-emerald-950">Semester</h3>@forelse($year->semesters as $semester)<p class="mt-2 text-sm">{{ $semester->name }} — {{ $semester->is_active ? 'Aktif' : 'Tidak Aktif' }}</p>@empty<div class="empty-state">Belum ada semester untuk tahun ajaran ini.</div>@endforelse</div></section></div></x-app-layout>
