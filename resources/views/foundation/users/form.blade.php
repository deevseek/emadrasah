<x-app-layout><form method="post" action="{{ $user->exists ? route('users.update',$user) : route('users.store') }}">@csrf @if($user->exists) @method('put') @endif<input name="name" value="{{ old('name',$user->name) }}"><input name="email" value="{{ old('email',$user->email) }}"><input name="password" type="password">@foreach($roles as $role)<label><input type="checkbox" name="roles[]" value="{{ $role->id }}" @checked($user->roles->contains($role))>{{ $role->display_name }}</label>@endforeach<button>Simpan</button></form></x-app-layout>
