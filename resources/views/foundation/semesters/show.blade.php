<x-app-layout title="Detail Semester"><section class="card"><div class="card-body"><h2 class="text-xl font-bold text-emerald-950">Semester {{ $semester->name }}</h2><p class="text-sm text-slate-500">Tahun ajaran {{ $semester->academicYear?->name }}</p><p class="mt-2 text-sm">{{ $semester->starts_on->format('d/m/Y') }} - {{ $semester->ends_on->format('d/m/Y') }}</p><span @class(['badge','mt-3',$semester->is_active?'badge-success':'badge-muted'])>{{ $semester->is_active?'Aktif':'Tidak Aktif' }}</span><div class="mt-4 flex gap-2"><a class="btn btn-secondary" href="{{ route('semesters.index') }}">Kembali</a>@can('semesters.update')<a class="btn btn-primary" href="{{ route('semesters.edit',$semester) }}">Ubah</a>@endcan</div></div></section></x-app-layout>
