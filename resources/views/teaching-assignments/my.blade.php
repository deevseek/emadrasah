<x-layouts.app title="Tugas Mengajar Saya" :breadcrumbs="['Beranda','Tugas Mengajar Saya']"><div class="space-y-6"><x-ui.page-header title="Tugas Mengajar Saya" description="Ringkasan tugas dari pembagian tugas yang telah diaktifkan."/>@if(!$personnel)<x-ui.alert type="warning">Akun Anda belum terhubung dengan Data Personalia. Hubungi operator madrasah.</x-ui.alert>@elseif(!$set)<x-ui.card><p>Pembagian tugas mengajar belum diaktifkan.</p></x-ui.card>@else<x-ui.card><h2 class="font-black text-emerald-950">{{ $personnel->full_name }}</h2><p>Tahun Ajaran {{ $set->academicYear->name }} · Status: Aktif</p><div class="mt-4 grid gap-3 sm:grid-cols-4"><div>Total JP<strong class="block text-2xl">{{ $report['total'] }}</strong></div><div>Rombel<strong class="block text-2xl">{{ $report['classroom_count'] }}</strong></div><div>Mata Pelajaran<strong class="block text-2xl">{{ $report['subject_count'] }}</strong></div><div>JP Tugas Tambahan<strong class="block text-2xl">{{ $report['additional'] }}</strong></div></div></x-ui.card><div class="grid gap-4 md:grid-cols-2">@foreach($report['assignments'] as $assignment)<x-ui.card><strong>{{ $assignment->subject->name }}</strong><p>{{ $assignment->classroom->display_name }} · {{ $assignment->weekly_periods }} JP</p></x-ui.card>@endforeach</div><x-ui.card><h3 class="font-bold">Tugas Tambahan</h3>@forelse($report['duties'] as $duty)<p>{{ $duty->duty_name }} · {{ $duty->equivalent_periods }} JP</p>@empty<p>Tidak ada tugas tambahan.</p>@endforelse</x-ui.card>@can('teaching-assignments.print-own')<a class="btn btn-primary w-full sm:w-auto" href="{{ route('teaching-assignments.personnel.print',[$personnel,'set'=>$set->id]) }}">Cetak Pembagian Tugas Saya</a>@endcan @endif</div></x-layouts.app>
