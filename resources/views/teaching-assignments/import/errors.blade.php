<x-layouts.app title="Daftar Error Import" :breadcrumbs="['Beranda','Akademik','Import Pembagian Tugas','Daftar Error']"><div class="space-y-6"><x-ui.page-header title="Daftar Data yang Perlu Ditindaklanjuti" description="Periksa data yang belum cocok, memiliki beberapa kandidat, atau memerlukan pemeriksaan manual."><x-slot:actions><a class="btn btn-secondary" href="{{ route('teaching-assignments.import.show',$batch) }}">Kembali ke Pratinjau</a></x-slot:actions></x-ui.page-header><x-ui.card><div class="space-y-3">@forelse($rows as $row)<div class="rounded-xl border border-slate-200 p-4"><strong>{{ $row->sheet_name }} — baris {{ $row->row_number }}</strong><p class="text-sm text-slate-600">{{ ['unmatched'=>'Belum Cocok','selection'=>'Perlu Dipilih','review'=>'Perlu Diperiksa'][$row->status] }}</p><p class="mt-1 text-sm">{{ implode(' ', $row->messages ?? []) }}</p></div>@empty<p class="text-slate-500">Tidak ada error pencocokan.</p>@endforelse</div><div class="mt-4">{{ $rows->links() }}</div></x-ui.card></div></x-layouts.app>
