<x-layouts.app title="Mata Pelajaran" :breadcrumbs="['Beranda','Data Madrasah','Mata Pelajaran']"><div class="space-y-6"><x-ui.page-header title="Daftar Mata Pelajaran" description="Kelola master mata pelajaran yang digunakan dalam struktur kurikulum."><x-slot:actions><div class="flex gap-2">@can('subjects.export')<a class="btn btn-secondary" href="{{ route('subjects.export') }}">Export XLSX</a>@endcan @can('subjects.view-loads')<a class="btn btn-secondary" href="{{ route('subjects.loads.edit') }}">Matriks Struktur JP</a>@endcan @can('subjects.create')<a class="btn btn-primary" href="{{ route('subjects.create') }}">Tambah Mata Pelajaran</a>@endcan</div></x-slot:actions></x-ui.page-header><x-ui.card><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead><tr class="border-b"><th class="p-3">Kode</th><th class="p-3">Mata Pelajaran</th><th class="p-3">Kategori</th><th class="p-3">Status</th><th class="p-3"></th></tr></thead><tbody>@forelse($subjects as $subject)<tr class="border-b"><td class="p-3 font-mono">{{ $subject->code }}</td><td class="p-3 font-semibold">{{ $subject->name }}</td><td class="p-3">{{ $subject->category ?: '—' }}</td><td class="p-3">{{ $subject->is_active ? 'Aktif' : 'Nonaktif' }}</td><td class="p-3 text-right">@can('subjects.update')<a class="btn btn-secondary" href="{{ route('subjects.edit',$subject) }}">Edit</a>@endcan</td></tr>@empty<tr><td colspan="5" class="p-6 text-center text-slate-500">Belum ada mata pelajaran.</td></tr>@endforelse</tbody></table></div></x-ui.card></div></x-layouts.app>
