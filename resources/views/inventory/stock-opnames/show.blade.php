<x-inventory.layout :title="'Stock Opname '.$opname->opname_number"><form method="post" action="{{ route('inventory.stock-opnames.update',$opname) }}" class="card card-body">@csrf @method('put')<table class="min-w-full text-sm"><tr><th>Barang</th><th>Kondisi</th><th>Sistem</th><th>Fisik</th><th>Selisih</th><th>Catatan</th></tr>@foreach($opname->lines as $l)<tr class="border-t"><td>{{ $l->item?->name }}</td><td>{{ $l->condition?->name }}</td><td>{{ $l->system_quantity }}</td><td><input type="number" name="lines[{{ $l->id }}][physical_quantity]" value="{{ $l->physical_quantity }}" @disabled($opname->status==='completed')></td><td>{{ $l->difference_quantity }}</td><td><input name="lines[{{ $l->id }}][notes]" value="{{ $l->notes }}" @disabled($opname->status==='completed')></td></tr>@endforeach</table>@if($opname->status!=='completed')<button class="btn btn-primary">Simpan Hasil Fisik</button></form><form method="post" action="{{ route('inventory.stock-opnames.post',$opname) }}">@csrf @method('patch')<button class="btn btn-secondary" onclick="return confirm('Posting stock opname dan buat koreksi?')">Posting Stock Opname</button></form>@else</form>@endif</x-inventory.layout>
