<x-inventory.layout title="Buat Stock Opname"><form method="post" action="{{ route('inventory.stock-opnames.store') }}" class="card card-body grid gap-4 md:grid-cols-2">@csrf<select name="inventory_location_id">@foreach($locations as $l)<option value="{{ $l->id }}">{{ $l->name }}</option>@endforeach</select><input type="date" name="opname_date" value="{{ now()->toDateString() }}"><textarea name="notes" placeholder="Catatan"></textarea><button class="btn btn-primary">Ambil Snapshot</button></form></x-inventory.layout>
