<x-inventory.layout title="Riwayat Transaksi Inventaris"><a class="btn btn-primary" href="{{ route('inventory.transactions.create') }}">Catat Transaksi</a><div class="card overflow-x-auto"><table class="min-w-full text-sm"><tr><th>Nomor</th><th>Tanggal</th><th>Jenis</th><th>Barang</th><th>Jumlah</th><th>Lokasi</th><th>Status</th><th>Aksi</th></tr>@foreach($transactions as $t)<tr class="border-t"><td>{{ $t->transaction_number }}</td><td>{{ $t->transaction_date->format('d/m/Y') }}</td><td>{{ $t->type }}</td><td>{{ $t->item?->name }}</td><td>{{ $t->quantity }}</td><td>{{ $t->fromLocation?->name }} → {{ $t->toLocation?->name }}</td><td>{{ $t->status }}</td><td>@can('inventory.reverse')<form method="post" action="{{ route('inventory.transactions.reverse',$t) }}">@csrf @method('patch')<input name="reason" required placeholder="Alasan reversal"><button class="btn btn-secondary" onclick="return confirm('Batalkan transaksi?')">Reversal</button></form>@endcan</td></tr>@endforeach</table>{{ $transactions->links() }}</div></x-inventory.layout>
