<x-inventory.layout title="Catat Transaksi Inventaris"><form method="post" action="{{ route('inventory.transactions.store') }}" class="card card-body grid gap-4 md:grid-cols-2">@csrf<select name="type"><option value="opening">Saldo awal</option><option value="addition">Penambahan</option><option value="reduction">Pengurangan</option><option value="transfer">Mutasi lokasi</option><option value="condition_change">Perubahan kondisi</option><option value="correction">Koreksi</option></select><input type="date" name="transaction_date" value="{{ now()->toDateString() }}"><select name="inventory_item_id">@foreach($items as $i)<option value="{{ $i->id }}" @selected(request('item_id')==$i->id)>{{ $i->code }} - {{ $i->name }}</option>@endforeach</select><input type="number" min="1" name="quantity" required placeholder="Jumlah"><select name="from_location_id"><option value="">Lokasi asal</option>@foreach($locations as $l)<option value="{{ $l->id }}">{{ $l->name }}</option>@endforeach</select><select name="to_location_id"><option value="">Lokasi tujuan</option>@foreach($locations as $l)<option value="{{ $l->id }}">{{ $l->name }}</option>@endforeach</select><select name="from_condition_id"><option value="">Kondisi awal</option>@foreach($conditions as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</select><select name="to_condition_id"><option value="">Kondisi akhir</option>@foreach($conditions as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</select><input name="reason" required placeholder="Alasan/keterangan"><textarea name="notes" placeholder="Catatan"></textarea><button class="btn btn-primary" data-disable-on-submit>Posting</button>@foreach($errors->all() as $e)<p class="text-red-600">{{ $e }}</p>@endforeach</form></x-inventory.layout>
