<x-inventory.layout :title="$item->name"><section class="card card-body"><p>{{ $item->code }} · {{ $item->category?->name }} · {{ $item->unit?->name }}</p><p>Nilai perolehan: Rp {{ number_format((float)$item->acquisition_value,0,',','.') }}</p><a class="btn btn-secondary" href="{{ route('inventory.transactions.create',['item_id'=>$item->id]) }}">Catat Transaksi</a></section><section class="card card-body"><h3 class="font-bold">Distribusi Saldo</h3>@foreach($item->balances as $b)<p>{{ $b->location?->name }} · {{ $b->condition?->name }} · {{ $b->quantity }}</p>@endforeach</section><section class="card card-body"><h3 class="font-bold">Riwayat Transaksi</h3>@foreach($item->transactions as $t)<p>{{ $t->transaction_date->format('d/m/Y') }} · {{ $t->type }} · {{ $t->quantity }} · {{ $t->reason }}</p>@endforeach</section></x-inventory.layout>
