<x-inventory.layout title="Data Barang Inventaris"><div class="flex gap-2"><a class="btn btn-primary" href="{{ route('inventory.items.create') }}">Tambah Barang</a><a class="btn btn-secondary" href="{{ route('inventory.reports.print',request()->query()) }}">Print</a><a class="btn btn-secondary" href="{{ route('inventory.reports.export',request()->query()) }}">Export CSV</a></div><form class="card card-body grid gap-3 md:grid-cols-4"><input name="q" value="{{ request('q') }}" placeholder="Cari kode/nama"><select name="category_id"><option value="">Semua kategori</option>@foreach($categories as $c)<option value="{{ $c->id }}" @selected(request('category_id')==$c->id)>{{ $c->name }}</option>@endforeach</select><button class="btn btn-primary">Terapkan</button></form><div class="overflow-x-auto card"><table class="min-w-full text-sm"><tr><th>Kode</th><th>Nama</th><th>Kategori</th><th>Satuan</th><th>Total</th><th>Lokasi</th><th>Status</th><th>Aksi</th></tr>@forelse($items as $i)<tr class="border-t"><td>{{ $i->code }}</td><td>{{ $i->name }}</td><td>{{ $i->category?->name }}</td><td>{{ $i->unit?->name }}</td><td>{{ $i->balances_sum_quantity ?? 0 }}</td><td>{{ $i->primaryLocation?->name ?? '-' }}</td><td>{{ $i->is_active?'Aktif':'Nonaktif' }}</td><td><a href="{{ route('inventory.items.show',$i) }}">Detail</a> @can('inventory.update')<a href="{{ route('inventory.items.edit',$i) }}">Edit</a>@endcan</td></tr>@empty<tr><td colspan="8" class="p-6 text-center">Belum ada barang inventaris.</td></tr>@endforelse</table>{{ $items->links() }}</div></x-inventory.layout>
