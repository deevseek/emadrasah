<x-inventory.layout title="Kondisi Barang"><div class="card overflow-x-auto"><table class="min-w-full text-sm"><tr><th>Kode</th><th>Label</th><th>Severity</th><th>Status</th><th>Aksi</th></tr>@foreach($conditions as $c)<tr class="border-t"><td>{{ $c->code }}</td><td>{{ $c->name }}</td><td>{{ $c->severity }}</td><td>{{ $c->is_active?'Aktif':'Nonaktif' }}</td><td><form method="post" action="{{ route('inventory.conditions.update',$c) }}" class="flex gap-2">@csrf @method('put')<input name="name" value="{{ $c->name }}"><input name="severity" value="{{ $c->severity }}"><input name="display_order" type="number" value="{{ $c->display_order }}"><label><input type="checkbox" name="is_active" value="1" @checked($c->is_active)> Aktif</label><button class="btn btn-secondary">Update</button></form></td></tr>@endforeach</table>{{ $conditions->links() }}</div></x-inventory.layout>
