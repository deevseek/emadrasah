<x-app-layout title="Jurnal Kelas"><div class="space-y-6"><x-ui.page-header title="Jurnal Kelas" description="Catatan kondisi dan kegiatan umum rombel setiap hari."/>@can('classroom-journals.manage')<a class="btn btn-primary" href="{{route('academic.classroom-journals.create')}}">Isi Jurnal Kelas</a>@endcan<form class="card card-body grid gap-3 md:grid-cols-4"><select class="input" name="academic_year_id">@foreach($years as $x)<option value="{{$x->id}}" @selected($year==$x->id)>{{$x->name}}</option>@endforeach</select><select class="input" name="semester_id">@foreach($semesters as $x)<option value="{{$x->id}}" @selected($semester==$x->id)>{{$x->display_name}}</option>@endforeach</select><input class="input" type="date" name="journal_date" value="{{$date}}"><select class="input" name="classroom_id"><option value="">Semua rombel</option>@foreach($rooms as $x)<option value="{{$x->id}}">{{$x->display_name}}</option>@endforeach</select><button class="btn btn-primary md:col-span-4">Terapkan Filter</button></form><div class="grid gap-4 md:grid-cols-2">@forelse($journals as $j)<a class="card card-body" href="{{route('academic.classroom-journals.show',$j)}}"><div class="flex justify-between"><strong>{{$j->classroom->display_name}}</strong><span class="badge badge-success">Tercatat</span></div><p class="text-sm text-slate-500">{{$j->journal_date->translatedFormat('d F Y')}} · Wali Kelas: {{$j->classroom->homeroom_name}}</p><p class="line-clamp-2">{{$j->important_events?:'Tidak ada kejadian penting.'}}</p></a>@empty<x-ui.empty-state title="Belum ada jurnal kelas pada filter ini."/>@endforelse</div>{{$journals->links()}}</div></x-app-layout>
