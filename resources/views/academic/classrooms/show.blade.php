<x-app-layout title="Detail Kelas & Rombel"><div class="space-y-6"><div class="flex justify-between gap-3"><div><h2 class="text-2xl font-black text-emerald-950">{{ $classroom->code }} — {{ $classroom->name }}</h2><p class="text-sm text-slate-500">Ringkasan kelas, daftar siswa aktif, riwayat penempatan, dan wali kelas.</p></div><a class="btn btn-secondary" href="{{ route('classrooms.index') }}">Kembali</a></div><div class="grid gap-3 md:grid-cols-4"><div class="card card-body"><p class="text-sm text-slate-500">Tingkat</p><p class="font-bold">{{ $classroom->gradeLevel?->name }}</p></div><div class="card card-body"><p class="text-sm text-slate-500">Tahun Ajaran</p><p class="font-bold">{{ $classroom->academicYear?->name }}</p></div><div class="card card-body"><p class="text-sm text-slate-500">Wali Kelas</p><p class="font-bold">{{ $classroom->homeroomTeacher?->name ?? 'Belum ditetapkan' }}</p></div><div class="card card-body"><p class="text-sm text-slate-500">Kapasitas</p><p class="font-bold">{{ $classroom->active_student_enrollments_count }}/{{ $classroom->capacity }} siswa, sisa {{ $classroom->remainingSeats() }}</p></div></div><div class="flex flex-wrap gap-2">@can('classrooms.update')<a class="btn btn-secondary" href="{{ route('classrooms.edit',$classroom) }}">Edit Kelas</a>@endcan @can('homeroom-assignments.manage')<a class="btn btn-secondary" href="{{ route('classrooms.homeroom.edit',$classroom) }}">Tetapkan/Ganti Wali Kelas</a>@endcan @can('student-enrollments.create')<a class="btn btn-primary" href="{{ route('classrooms.placements.create',$classroom) }}">Tambahkan Siswa</a>@endcan @can('student-enrollments.export')<a class="btn btn-secondary" href="{{ route('classrooms.students.export',$classroom) }}">Export Daftar Siswa XLSX</a>@endcan @can('classrooms.activate')<form method="POST" action="{{ route('classrooms.toggle',$classroom) }}" onsubmit="return confirm('Ubah status kelas?')">@csrf @method('PATCH')<button class="btn btn-secondary">{{ $classroom->is_active ? 'Nonaktifkan' : 'Aktifkan' }}</button></form>@endcan</div><section class="card card-body"><h3 class="font-bold text-emerald-950">Daftar Siswa Aktif</h3>@if($activeEnrollments->isEmpty())<div class="empty-state">Belum ada siswa aktif di kelas ini.</div>@else<div class="table-wrap"><table class="data-table"><thead><tr><th>Nama</th><th>NIS</th><th>NISN</th><th>Tanggal Mulai</th><th>Tindakan</th></tr></thead><tbody>@foreach($activeEnrollments as $enrollment)<tr><td>{{ $enrollment->student?->name }}</td><td>{{ $enrollment->student?->student_number }}</td><td>{{ $enrollment->student?->national_student_number }}</td><td>{{ optional($enrollment->enrolled_at)->format('d/m/Y') }}</td><td>@can('student-enrollments.transfer')<a class="btn btn-secondary px-3 py-1.5" href="{{ route('student-enrollments.transfer.edit',$enrollment) }}">Pindahkan Siswa</a>@endcan</td></tr>@endforeach</tbody></table></div>@endif</section><section class="card card-body"><h3 class="font-bold text-emerald-950">Riwayat Penempatan</h3><div class="table-wrap"><table class="data-table"><thead><tr><th>Siswa</th><th>Status</th><th>Mulai</th><th>Selesai</th><th>Keterangan</th></tr></thead><tbody>@forelse($historyEnrollments as $enrollment)<tr><td>{{ $enrollment->student?->name }}</td><td>{{ $enrollment->enrollment_status?->label() }}</td><td>{{ optional($enrollment->enrolled_at)->format('d/m/Y') }}</td><td>{{ optional($enrollment->completed_at)->format('d/m/Y') ?? '-' }}</td><td>{{ $enrollment->notes ?? '-' }}</td></tr>@empty<tr><td colspan="5" class="text-center text-slate-500">Belum ada riwayat.</td></tr>@endforelse</tbody></table></div>{{ $historyEnrollments->links() }}</section><section class="card card-body"><h3 class="font-bold text-emerald-950">Riwayat Wali Kelas</h3><ul class="space-y-2">@forelse($classroom->homeroomAssignments as $assignment)<li>{{ $assignment->employee?->name }} — {{ $assignment->started_at?->format('d/m/Y') }} s.d. {{ $assignment->ended_at?->format('d/m/Y') ?? 'aktif' }} ({{ $assignment->reason ?? '-' }})</li>@empty<li class="text-slate-500">Belum ada riwayat wali kelas.</li>@endforelse</ul></section><section class="card card-body"><h3 class="font-bold text-emerald-950">Aktivitas Terakhir</h3><ul>@foreach($activities as $activity)<li class="text-sm text-slate-600">{{ $activity->created_at }} — {{ $activity->description ?? $activity->event }}</li>@endforeach</ul></section></div></x-app-layout>
