<x-app-layout title="{{ $classroom->exists ? 'Edit Kelas/Rombel' : 'Tambah Kelas/Rombel' }}"><div class="space-y-6"><div><h2 class="text-2xl font-black text-emerald-950">{{ $classroom->exists ? 'Edit Kelas/Rombel' : 'Tambah Kelas/Rombel' }}</h2><p class="text-sm text-slate-500">Pastikan kode unik dalam satu tahun ajaran dan kapasitas sesuai kondisi kelas.</p></div><form method="POST" action="{{ $classroom->exists ? route('classrooms.update',$classroom) : route('classrooms.store') }}" class="card card-body grid gap-4 md:grid-cols-2">@csrf @if($classroom->exists) @method('PUT') @endif<label>Tahun Ajaran<select name="academic_year_id">@foreach($academicYears as $year)<option value="{{ $year->id }}" @selected(old('academic_year_id',$classroom->academic_year_id)==$year->id)>{{ $year->name }}</option>@endforeach</select>@error('academic_year_id')<p class="text-sm text-red-600">{{ $message }}</p>@enderror</label><label>Tingkat Kelas<select name="grade_level_id">@foreach($gradeLevels as $grade)<option value="{{ $grade->id }}" @selected(old('grade_level_id',$classroom->grade_level_id)==$grade->id)>{{ $grade->name }}</option>@endforeach</select>@error('grade_level_id')<p class="text-sm text-red-600">{{ $message }}</p>@enderror</label><label>Nama Rombel<input name="name" value="{{ old('name',$classroom->name) }}">@error('name')<p class="text-sm text-red-600">{{ $message }}</p>@enderror</label><label>Kode Rombel<input name="code" value="{{ old('code',$classroom->code) }}">@error('code')<p class="text-sm text-red-600">{{ $message }}</p>@enderror</label><label>Kapasitas<input type="number" min="1" max="60" name="capacity" value="{{ old('capacity',$classroom->capacity ?? 28) }}">@error('capacity')<p class="text-sm text-red-600">{{ $message }}</p>@enderror</label><label>Ruang Kelas<input name="room" value="{{ old('room',$classroom->room) }}"></label><label class="md:col-span-2">Keterangan<textarea name="description">{{ old('description',$classroom->description) }}</textarea></label><label class="flex items-center gap-2"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$classroom->is_active ?? true))> Aktif</label><div class="md:col-span-2 flex gap-2"><button class="btn btn-primary" data-disable-on-submit>Simpan</button><a class="btn btn-secondary" href="{{ $classroom->exists ? route('classrooms.show',$classroom) : route('classrooms.index') }}">Kembali</a></div></form></div></x-app-layout>
