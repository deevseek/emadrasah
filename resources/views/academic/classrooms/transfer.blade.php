<x-app-layout title="Pindahkan Siswa"><div class="space-y-6"><div><h2 class="text-2xl font-black text-emerald-950">Pindahkan Siswa</h2><p class="text-sm text-slate-500">{{ $enrollment->student?->name }} dari {{ $enrollment->classroom?->code }}.</p></div><form method="POST" action="{{ route('student-enrollments.transfer',$enrollment) }}" class="card card-body grid gap-4" onsubmit="return confirm('Pindahkan siswa dan tutup riwayat lama?')">@csrf<label>Kelas Tujuan<select name="target_classroom_id">@foreach($targets as $target)<option value="{{ $target->id }}">{{ $target->code }} — {{ $target->name }}</option>@endforeach</select>@error('target_classroom_id')<p class="text-sm text-red-600">{{ $message }}</p>@enderror</label><label>Tanggal Efektif<input type="date" name="effective_date" value="{{ old('effective_date', now()->toDateString()) }}">@error('effective_date')<p class="text-sm text-red-600">{{ $message }}</p>@enderror</label><label>Alasan<textarea name="reason">{{ old('reason') }}</textarea>@error('reason')<p class="text-sm text-red-600">{{ $message }}</p>@enderror</label><label>Keterangan<textarea name="notes">{{ old('notes') }}</textarea></label><div class="flex gap-2"><button class="btn btn-primary" data-disable-on-submit>Pindahkan Siswa</button><a class="btn btn-secondary" href="{{ route('classrooms.show',$enrollment->classroom) }}">Kembali</a></div></form></div></x-app-layout>
