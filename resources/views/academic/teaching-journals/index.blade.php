<x-app-layout title="Jurnal Mengajar"><div class="index-stack"><x-ui.page-header title="Jurnal Mengajar" description="Catatan kegiatan guru untuk satu mata pelajaran."/>@can('teaching-journals.manage')<a class="btn btn-primary" href="{{route('academic.teaching-journals.create')}}">Isi Jurnal Mengajar</a>@endcan<form class="filter-bar grid gap-3 md:grid-cols-3"><label>Tahun Ajaran<select class="input" name="academic_year_id">@foreach($years as $x)<option value="{{$x->id}}" @selected($year==$x->id)>{{$x->name}}</option>@endforeach</select></label><label>Semester<select class="input" name="semester_id">@foreach($semesters as $x)<option value="{{$x->id}}" @selected($semester==$x->id)>{{$x->display_name}}</option>@endforeach</select></label><label>Tanggal<input class="input" type="date" name="journal_date" value="{{$date}}"></label><label>Rombel<select class="input" name="classroom_id"><option value="">Semua rombel</option>@foreach($rooms as $x)<option value="{{$x->id}}">{{$x->display_name}}</option>@endforeach</select></label><label>Mata Pelajaran<select class="input" name="academic_subject_id"><option value="">Semua mata pelajaran</option>@foreach($subjects as $x)<option value="{{$x->id}}">{{$x->name}}</option>@endforeach</select></label>@can('teaching-journals.view-all')<label>Guru<select class="input" name="personnel_id"><option value="">Semua guru</option>@foreach($teachers as $x)<option value="{{$x->id}}">{{$x->full_name}}</option>@endforeach</select></label>@endcan<button class="btn btn-primary md:col-span-3">Terapkan Filter</button></form><div class="grid gap-4 md:grid-cols-2">@forelse($journals as $j)<a href="{{route('academic.teaching-journals.show',$j)}}" class="card p-4"><div class="flex justify-between"><strong>{{$j->subject->name}}</strong><span class="badge badge-success">Tercatat</span></div><p class="mt-1 text-sm"><span class="text-slate-500">Materi:</span> {{$j->topic}}</p><p class="text-sm text-slate-500">{{$j->journal_date->translatedFormat('d F Y')}} · {{$j->classroom->display_name}} · {{$j->personnel->full_name}}</p><span class="mt-3 block text-right text-sm font-semibold text-emerald-700">Lihat Detail →</span></a>@empty<x-ui.empty-state title="Belum ada jurnal mengajar pada filter ini."/>@endforelse</div>{{$journals->links()}}</div></x-app-layout>
