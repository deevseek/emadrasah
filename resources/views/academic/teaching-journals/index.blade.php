<x-app-layout title="Jurnal Mengajar"><div class="space-y-6"><x-ui.page-header title="Jurnal Mengajar" description="Catatan kegiatan guru untuk satu mata pelajaran."/>@can('teaching-journals.manage')<a class="btn btn-primary" href="{{route('academic.teaching-journals.create')}}">Isi Jurnal Mengajar</a>@endcan<form class="card card-body grid gap-3 md:grid-cols-3"><select class="input" name="academic_year_id">@foreach($years as $x)<option value="{{$x->id}}" @selected($year==$x->id)>{{$x->name}}</option>@endforeach</select><select class="input" name="semester_id">@foreach($semesters as $x)<option value="{{$x->id}}" @selected($semester==$x->id)>{{$x->display_name}}</option>@endforeach</select><input class="input" type="date" name="journal_date" value="{{$date}}"><select class="input" name="classroom_id"><option value="">Semua rombel</option>@foreach($rooms as $x)<option value="{{$x->id}}">{{$x->display_name}}</option>@endforeach</select><select class="input" name="academic_subject_id"><option value="">Semua mata pelajaran</option>@foreach($subjects as $x)<option value="{{$x->id}}">{{$x->name}}</option>@endforeach</select>@can('teaching-journals.view-all')<select class="input" name="personnel_id"><option value="">Semua guru</option>@foreach($teachers as $x)<option value="{{$x->id}}">{{$x->full_name}}</option>@endforeach</select>@endcan<button class="btn btn-primary md:col-span-3">Terapkan Filter</button></form><div class="grid gap-4 md:grid-cols-2">@forelse($journals as $j)<a href="{{route('academic.teaching-journals.show',$j)}}" class="card card-body"><div class="flex justify-between"><strong>{{$j->subject->name}}</strong><span class="badge badge-success">Tercatat</span></div><p>{{$j->topic}}</p><p class="text-sm text-slate-500">{{$j->journal_date->translatedFormat('d F Y')}} · {{$j->classroom->display_name}} · {{$j->personnel->full_name}}</p><p class="text-sm">Pertemuan/Jam: {{$j->lesson_number?:'—'}}</p></a>@empty<x-ui.empty-state title="Belum ada jurnal mengajar pada filter ini."/>@endforelse</div>{{$journals->links()}}</div></x-app-layout>
