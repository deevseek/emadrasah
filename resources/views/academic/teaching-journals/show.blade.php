<x-app-layout title="Detail Jurnal Mengajar"><div class="space-y-5"><x-ui.page-header title="{{$journal->topic}}" description="{{$journal->classroom->display_name}} · {{$journal->subject->name}} · {{$journal->personnel->full_name}}"/><section class="card card-body"><div class="grid gap-3 md:grid-cols-3"><p><strong>Hari/Tanggal</strong><br>{{$journal->journal_date->translatedFormat('l, d F Y')}}</p><p><strong>Jam ke</strong><br>{{$journal->lesson_number?:'—'}}</p><p><strong>Metode Pembelajaran</strong><br>{{$journal->learning_method?:'—'}}</p></div>@foreach(['learning_objectives'=>'Tujuan Pembelajaran','learning_material'=>'Materi Pembelajaran','learning_activity'=>'Kegiatan Pembelajaran','assignment'=>'Tugas','notes'=>'Keterangan/Catatan'] as $key=>$label)<h2 class="mt-4 font-bold">{{$label}}</h2><p class="whitespace-pre-line">{{$journal->$key?:'—'}}</p>@endforeach</section><section class="card card-body"><h2 class="font-bold">Absensi Siswa dalam Jurnal</h2><div class="my-3 grid grid-cols-4 gap-2">@foreach(['present'=>'Hadir','sick'=>'Sakit','permitted'=>'Izin','absent'=>'Alpa'] as $key=>$label)<div class="rounded bg-slate-50 p-2"><small>{{$label}}</small><strong class="block text-xl text-emerald-900">{{$attendance[$key]??0}}</strong></div>@endforeach</div><div class="overflow-x-auto"><table class="w-full text-sm"><thead><tr class="bg-emerald-900 text-white"><th class="p-2 text-left">Nama</th><th class="p-2 text-left">Status</th><th class="p-2 text-left">Keterangan</th></tr></thead><tbody>@foreach($journal->attendances->sortBy('student.full_name') as $row)<tr class="border-b"><td class="p-2">{{$row->student->full_name}}</td><td class="p-2">{{$row->status->label()}}</td><td class="p-2">{{$row->notes?:'—'}}</td></tr>@endforeach</tbody></table></div></section>@can('teaching-journals.manage')<a class="btn btn-primary" href="{{route('academic.teaching-journals.edit',$journal)}}">Ubah</a><form class="inline" method="post" action="{{route('academic.teaching-journals.destroy',$journal)}}">@csrf @method('delete')<button class="btn btn-secondary">Hapus</button></form>@endcan</div></x-app-layout>
