<x-app-layout title="Detail Jurnal Mengajar"><div class="space-y-5"><x-ui.page-header title="{{$journal->topic}}" description="{{$journal->classroom->display_name}} · {{$journal->subject->name}} · {{$journal->personnel->full_name}}"/><section class="card card-body"><p>{{$journal->journal_date->translatedFormat('d F Y')}} · Pertemuan/Jam {{$journal->lesson_number?:'—'}}</p>@foreach(['learning_objectives'=>'Tujuan Pembelajaran','learning_material'=>'Materi Pembelajaran','learning_method'=>'Metode','learning_activity'=>'Kegiatan Pembelajaran','assignment'=>'Tugas','notes'=>'Catatan'] as $key=>$label)<h2 class="mt-4 font-bold">{{$label}}</h2><p class="whitespace-pre-line">{{$journal->$key?:'—'}}</p>@endforeach</section><section class="card card-body"><h2 class="font-bold">Ringkasan Absensi</h2><div class="grid grid-cols-4 gap-2">@foreach(['present'=>'Hadir','sick'=>'Sakit','permitted'=>'Izin','absent'=>'Alpa'] as $key=>$label)<div><small>{{$label}}</small><strong class="block">{{$attendance[$key]??0}}</strong></div>@endforeach</div></section>@can('teaching-journals.manage')<a class="btn btn-primary" href="{{route('academic.teaching-journals.edit',$journal)}}">Ubah</a><form class="inline" method="post" action="{{route('academic.teaching-journals.destroy',$journal)}}">@csrf @method('delete')<button class="btn btn-secondary">Hapus</button></form>@endcan</div></x-app-layout>
