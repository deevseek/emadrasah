<x-module-page title="Daftar Siswa Rapor" subtitle="Generate rapor siswa secara idempotent untuk semester terpilih.">
    <div class="rounded-xl bg-white p-6 shadow">@if (session('status'))<div class="mb-4 rounded-lg bg-emerald-50 p-4 text-emerald-900">{{ session('status') }}</div>@endif<div class="mb-4"><a href="{{ route('report-cards.classes') }}" class="rounded-lg border px-4 py-2">Kembali</a></div><div class="overflow-x-auto"><table class="min-w-full text-sm"><thead class="bg-gray-50"><tr><th class="px-3 py-2 text-left">Siswa</th><th class="px-3 py-2 text-left">Kelas</th><th class="px-3 py-2 text-left">Generate</th></tr></thead><tbody>@forelse($enrollments as $enrollment)<tr class="border-t"><td class="px-3 py-3">{{ $enrollment->student?->name }}</td><td class="px-3 py-3">{{ $classroom->name }}</td><td class="px-3 py-3"><form method="POST" action="{{ route('report-cards.generate', $enrollment) }}" class="flex gap-2">@csrf<input name="semester_id" class="w-28 rounded-lg border-slate-300" placeholder="ID Semester" required><button onclick="return confirm('Generate atau perbarui draft rapor siswa ini?')" class="rounded-lg bg-emerald-900 px-4 py-2 text-white">Generate Rapor</button></form></td></tr>@empty<tr><td colspan="3" class="px-3 py-8 text-center text-slate-500">Belum ada siswa aktif di kelas ini.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $enrollments->links() }}</div></div>
</x-module-page>
