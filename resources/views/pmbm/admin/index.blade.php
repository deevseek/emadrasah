<x-layouts.app title="Pendaftar PMBM" :breadcrumbs="['PMBM','Pendaftar']"><div class="index-stack"><x-ui.page-header title="Pendaftar PMBM" description="Kelola proses Penerimaan Murid Baru Madrasah."><a class="btn btn-primary" href="{{ route('pmbm.applicants.create') }}">+ Pendaftaran Offline</a></x-ui.page-header><x-ui.card><form class="flex gap-2"><input class="input" name="search" value="{{ request('search') }}" placeholder="Nomor, nama, atau NIK"><select class="input" name="status"><option value="">Semua status</option>@foreach($statuses as $status)<option value="{{ $status->value }}">{{ $status->label() }}</option>@endforeach</select><button class="btn btn-primary">Filter</button></form></x-ui.card><x-ui.card class="overflow-hidden !p-0"><table class="w-full text-sm"><thead class="bg-slate-50 text-left"><tr><th class="p-3">Pendaftar</th><th>Tahun</th><th>Sumber</th><th>Status</th><th></th></tr></thead><tbody>@forelse($applicants as $a)<tr class="border-t"><td class="p-3 font-semibold">{{ $a->full_name }}<small class="block text-slate-500">{{ $a->registration_number }}</small></td><td>{{ $a->academicYear->name }}</td><td class="capitalize">{{ $a->registration_source }}</td><td><span class="badge">{{ $a->status->label() }}</span></td><td><a class="font-bold text-emerald-800" href="{{ route('pmbm.applicants.show',$a) }}">Detail</a></td></tr>@empty<tr><td colspan="5" class="p-5 text-center text-slate-500">Belum ada pendaftar.</td></tr>@endforelse</tbody></table><div class="p-4">{{ $applicants->links() }}</div></x-ui.card></div></x-layouts.app>
