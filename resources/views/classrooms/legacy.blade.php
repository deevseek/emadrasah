<x-layouts.app title="Petakan Data Kelas Lama"><div class="space-y-5"><x-ui.page-header title="Petakan Data Kelas Lama" description="Hubungkan data kelas hasil impor dengan rombel resmi."/><x-ui.card><h2 class="font-bold">Panduan Pemetaan <x-ui.help-tip title="Data kelas lama" content="Label lama tidak akan dihapus setelah pemetaan."/></h2><p class="mt-2 text-sm">Langkah 1 — Periksa nama kelas lama · Langkah 2 — Pilih rombel tujuan · Langkah 3 — Periksa siswa · Langkah 4 — Konfirmasi</p></x-ui.card>@foreach($labels as $item)<x-ui.card><h3 class="font-bold">{{ $item['label'] }}</h3><p>{{ $item['total'] }} siswa · {{ $item['mapped']?'Sudah dipetakan':'Belum dipetakan' }}</p>@unless($item['mapped'])<form method="post" action="{{ route('classrooms.legacy.preview') }}" class="mt-3 flex flex-wrap gap-2">@csrf<input type="hidden" name="academic_year_id" value="{{ $year }}"><input type="hidden" name="legacy_label" value="{{ $item['label'] }}"><select class="input flex-1" name="classroom_id">@foreach($rooms as $room)<option value="{{ $room->id }}" @selected($item['suggestion']?->id===$room->id)>{{ $room->display_name }}</option>@endforeach</select><button class="btn btn-primary">Pratinjau</button></form>@endunless</x-ui.card>@endforeach</div></x-layouts.app>
