<x-layouts.app title="Pratinjau Salin Struktur"><form method="post" action="{{ route('classrooms.copy.confirm') }}" class="space-y-5">@csrf<x-ui.page-header title="Pratinjau Salin Struktur"/><x-ui.card>@foreach($rooms as $room)<p>✓ {{ $room->display_name }}</p><input type="hidden" name="classroom_ids[]" value="{{ $room->id }}">@endforeach</x-ui.card>@foreach($data as $k=>$v)@if(!is_array($v))<input type="hidden" name="{{ $k }}" value="{{ $v }}">@endif @endforeach<button class="btn btn-primary">Konfirmasi Salin Struktur</button></form></x-layouts.app>
