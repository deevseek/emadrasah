<x-layouts.app title="Atur Wali Kelas"><form method="post" action="{{ route('classrooms.homeroom.update',$classroom) }}" class="space-y-5">@csrf @method('PATCH')<x-ui.page-header title="Atur Wali Kelas" :description="$classroom->display_name"/><x-ui.card><p>Wali kelas saat ini: <strong>{{ $classroom->homeroom_name }}</strong></p><label class="mt-4 block"><span class="label">Pilih wali kelas baru <x-ui.help-tip title="Wali kelas" content="Guru tidak dapat menjadi wali dua rombel aktif pada tahun ajaran yang sama."/></span><select class="input" name="homeroom_personnel_id"><option value="">Kosongkan wali kelas</option>@foreach($personnel as $p)<option value="{{ $p->id }}" @selected($classroom->homeroom_personnel_id===$p->id)>{{ $p->full_name }} — {{ $p->position }}</option>@endforeach</select></label></x-ui.card><button class="btn btn-primary" onclick="return confirm('Ganti wali kelas? Perubahan akan dicatat dalam riwayat aktivitas.')">Simpan Wali Kelas</button></form></x-layouts.app>
