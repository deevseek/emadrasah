<x-app-layout :title="$title ?? 'Inventaris dan Aset'"><div class="space-y-6"><x-ui.page-header :title="$title ?? 'Inventaris dan Aset'" :description="$description ?? 'Kelola inventaris madrasah berdasarkan barang, lokasi, kondisi, saldo, transaksi, stock opname, laporan, print, dan export.'"><div class="flex flex-wrap gap-2"><a class="btn btn-secondary" href="{{ route('inventory.dashboard') }}">Dashboard</a><a class="btn btn-secondary" href="{{ route('inventory.items.index') }}">Barang</a><a class="btn btn-secondary" href="{{ route('inventory.transactions.index') }}">Transaksi</a><a class="btn btn-secondary" href="{{ route('inventory.reports.index') }}">Laporan</a></div></x-ui.page-header>@if(session('status'))<div class="rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-emerald-900">{{ session('status') }}</div>@endif {{ $slot }}</div></x-app-layout>
