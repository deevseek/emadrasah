<?php
declare(strict_types=1);namespace App\Http\Controllers\TeachingAssignments;use App\Http\Controllers\Controller;use App\Http\Requests\TeachingAssignments\ResolveTeachingConflictRequest;use App\Models\TeachingAssignmentSet;use App\Services\TeachingAssignments\TeachingAssignmentConflictService;use Illuminate\Http\RedirectResponse;
class TeachingAssignmentConflictController extends Controller {public function update(ResolveTeachingConflictRequest $r,TeachingAssignmentSet $set,TeachingAssignmentConflictService $s):RedirectResponse{$s->resolve($r->user(),$set,(int)$r->validated('classroom_id'),(int)$r->validated('subject_id'),$r->validated('strategy'),$r->validated('periods',[]),(int)$r->validated('primary_personnel_id'));return back()->with('success','Konflik pengampu berhasil diselesaikan.');}}
