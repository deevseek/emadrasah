# Development Setup

Gunakan PHP 8.2+, Laravel 12, Node 20+, Vite 7, Tailwind CSS 4, dan MySQL. Jalankan `composer install`, `npm install`, salin `.env.example` ke `.env`, isi kredensial lokal, `php artisan key:generate`, `php artisan migrate:fresh --seed`, `php artisan test`, dan `npm run build`.
